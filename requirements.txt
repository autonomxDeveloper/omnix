# Omnix - Requirements
# Install with: pip install -r requirements.txt

# ============================================
# CORE WEB FRAMEWORK
# ============================================
requests==2.31.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
pydantic>=2.5.0
websockets>=12.0
python-socketio>=5.9.0
aiohttp>=3.8.0

# ============================================
# MODEL DOWNLOADS
# ============================================
huggingface_hub>=0.25.0

# ============================================
# PYTORCH - CUDA 12.4 (CRITICAL VERSION MATCH)
# Install separately with:
# pip install torch==2.5.1+cu124 torchvision==0.20.1+cu124 torchaudio==2.5.1+cu124 --index-url https://download.pytorch.org/whl/cu124
# ============================================
# PyTorch is installed separately by bootstrap script
# Do NOT pin torch versions here - use bootstrap_omnix_flux_env.ps1

# ============================================
# AUDIO PROCESSING
# ============================================
numpy==1.26.4
packaging==23.2
wheel==0.43.0
scipy>=1.9.0
soundfile>=0.12.0
pydub==0.25.1
librosa==0.11.0

# Speech enhancement (optional - for studio-quality voice)
deepfilternet>=0.5.0

# ============================================
# NEMO ASR (PARAKEET STT)
# Install with: pip install nemo_toolkit[asr]
# Note: This will update transformers to 4.53.x
# ============================================
# nemo_toolkit[asr]>=2.5.0
# TTS moved to rpg-tts environment


# ============================================
# IMAGE GENERATION (optional - for flux_klein visual provider)
# ============================================
diffusers>=0.31.0
transformers>=4.40.0
safetensors>=0.4.0
# torch installed explicitly in setup.bat (DO NOT install via pip)

# ============================================
# DOCUMENT PROCESSING
# ============================================
PyPDF2>=3.0.0
python-docx>=1.1.0
pandas>=2.0.0

# ============================================
# TESTING
# ============================================
pytest>=7.0.0
pytest-cov>=4.0.0
playwright>=1.40.0
pytest-playwright>=0.4.0