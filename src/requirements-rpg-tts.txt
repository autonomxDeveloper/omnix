# Dedicated TTS service requirements for the rpg-tts environment.
# This environment owns the fragile TTS/model stack.
# The main app must not rely on these packages directly.

# Core service
fastapi>=0.100,<1
uvicorn[standard]>=0.23,<1
python-multipart>=0.0.6,<1
requests>=2.31,<3
pydantic>=2.5,<3

# Core numerical/audio
numpy==1.26.4
scipy>=1.9,<2
soundfile>=0.12,<1
librosa==0.11.0
sox>=1.4,<2
soxr>=0.3,<2
onnxruntime>=1.18,<2

# Proven missing runtime deps from prior failures
regex>=2024.0.0

# Torch stack for Windows / CUDA env
# Keep aligned with setup.bat torch install strategy if you install torch separately.
torch==2.5.1
torchaudio==2.5.1

# HF / tokenizer stack for vendored Qwen3-TTS
transformers==4.46.3
tokenizers==0.20.3
huggingface_hub==0.34.6
safetensors>=0.5,<0.8
sentencepiece>=0.2,<0.3
accelerate==1.12.0
packaging==23.2
setuptools==81.0.0
wheel==0.43.0
psutil>=5.9,<7

# Misc runtime helpers commonly required by tokenizer/model code
PyYAML>=6,<7
tqdm>=4.66,<5
filelock>=3.16,<4
einops>=0.8,<0.9
typing_extensions>=4.8,<5