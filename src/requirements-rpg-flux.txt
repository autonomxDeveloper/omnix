# Omnix optional RPG FLUX runtime requirements
# Installed by setup.bat into the rpg-flux conda environment.
#
# This file must fully pin the fragile HF stack used by FLUX runtime validation.
# Do not rely on broad requirements.txt ranges for these packages.

numpy==1.26.4
soundfile>=0.12,<1
Pillow>=10,<12

# FLUX / image stack
diffusers>=0.33,<0.36
accelerate==1.12.0
safetensors>=0.5,<0.7
huggingface_hub==0.34.6
transformers==4.57.3
tokenizers==0.22.2
psutil>=5.9,<7
regex>=2024.0.0

# Common utility/runtime deps used by vendored/model code
packaging==23.2
wheel==0.43.0
einops>=0.8,<0.9
filelock>=3.16,<4
requests>=2.31,<3
PyYAML>=6,<7
tqdm>=4.66,<5