# Omnix optional RPG FLUX/TTS runtime requirements
# Installed by setup.bat into the rpg-flux conda environment.
#
# Keep this file focused on the fragile optional runtime stack.
# Do not move broad app requirements here.

numpy==1.26.4
soundfile>=0.12,<1
Pillow>=10,<12

# FLUX / image stack
diffusers>=0.33,<0.36
accelerate==1.12.0
safetensors>=0.5,<0.7
huggingface_hub>=0.30,<0.35

# Common utility/runtime deps used by vendored/model code
packaging==23.2
wheel==0.43.0
einops>=0.8,<0.9
filelock>=3.16,<4
requests>=2.31,<3
PyYAML>=6,<7
tqdm>=4.66,<5