⚠️ LAST CRITICAL GAPS (Before Phase 2)

These are not bugs — they are scaling risks.

❗ 1. _active_loop Is Not Safe Long-Term
Problem
_active_loop = None

This:

breaks with async/multithreading later
breaks with multiple sessions (you will need this)
✅ FIX — CONTEXT-LOCAL LOOP (IMPORTANT)
🔧 FILE: src/app/rpg/core/game_loop.py
+ import contextvars
🔁 REPLACE
- _active_loop = None
+ _active_loop_ctx = contextvars.ContextVar("active_game_loop", default=None)
🔁 MODIFY tick()
- if GameLoop._active_loop and GameLoop._active_loop is not self:
-     raise RuntimeError("Multiple GameLoop instances detected")
-
- GameLoop._active_loop = self
+ current = GameLoop._active_loop_ctx.get()
+ if current and current is not self:
+     raise RuntimeError("Multiple GameLoop instances detected in same context")
+
+ token = GameLoop._active_loop_ctx.set(self)
➕ ADD FINALLY CLEANUP
+ try:
+     # existing tick logic
+     ...
+ finally:
+     GameLoop._active_loop_ctx.reset(token)

👉 This makes your system:

future-proof for async
safe for multiplayer sessions
❗ 2. EventBus Enforcement Is Still Passive

Right now:

assert_event_usage() = placeholder

✅ FIX — DETECT DIRECT CROSS-SYSTEM CALLS
🔧 FILE: src/app/rpg/core/event_bus.py
➕ ADD STRONGER CHECK
+ import sys
+    def assert_event_usage(self):
+        if not self._enforce:
+            return
+
+        stack = inspect.stack()
+
+        for frame in stack[2:]:  # skip emit + assert frames
+            module = inspect.getmodule(frame[0])
+            if not module:
+                continue
+
+            name = module.__name__
+
+            # Allow core systems
+            if name.startswith("app.rpg.core"):
+                continue
+
+            # Detect suspicious direct calls
+            if "npc" in name and "world" in name:
+                raise RuntimeError(
+                    f"Illegal cross-system call detected: {name}. "
+                    "Use EventBus instead."
+                )

👉 This is still basic — but now it’s real enforcement, not a stub.

❗ 3. EventBus Is Write-Only (Missing Replay / Debug Power)

Right now:

events are emitted
then lost after collect()
✅ FIX — ADD EVENT HISTORY (DEBUG SUPERPOWER)
🔧 FILE: src/app/rpg/core/event_bus.py
+        self._history: List[Event] = []
     def emit(self, event: Event) -> None:
         self.assert_event_usage()
+        self._history.append(event)
+    def history(self) -> List[Event]:
+        return self._history[:]

👉 This unlocks:

replay
debugging
narrative causality tracking (later 🔥)
❗ 4. StoryDirector Emits Only ONE Event Type

Right now:

"scene_generated"

👉 Too limited.

✅ FIX — ADD STRUCTURED EVENT TYPES
🔧 FILE: src/app/rpg/narrative/story_director.py
+        event_bus.emit(Event("narrative_beat_selected", {
+            "beat": next_beat,
+            "tick": self._tick_count
+        }))
+        event_bus.emit(Event("scene_generated", {
+            "tick": self._tick_count,
+            "beat": next_beat,
+            "scene": scene
+        }))

👉 This is crucial for:

debugging
analytics
future UI
❗ 5. No System Identity (You Can’t Trace Who Did What)

Right now events don’t include source system

✅ FIX — ADD source FIELD TO EVENT
🔧 FILE: src/app/rpg/core/event_bus.py
 @dataclass
 class Event:
     type: str
     payload: Dict[str, Any] = field(default_factory=dict)
+    source: Optional[str] = None
UPDATE EMITS

Example:

event_bus.emit(Event(
    "scene_generated",
    {...},
    source="story_director"
))

👉 This becomes critical in Phase 3+

❗ 6. No Tick ID in EventBus (Temporal Debugging Missing)
✅ FIX
🔧 FILE: game_loop.py
 events = self.event_bus.collect()
BEFORE collecting:
+ self.event_bus._current_tick = self._tick_count
UPDATE EVENT EMIT
+ event.payload["tick"] = getattr(self, "_current_tick", None)

👉 Now every event is:

timestamped
traceable
🏁 FINAL STATE AFTER THESE FIXES

You now have:

Capability	Level
Execution control	✅ strict
Communication	✅ enforced
Debugging	🔥 advanced
Replayability	🔥 enabled
Multi-session future	✅ safe
Observability	🔥 high