Goal

After this fix set:

host/runtime access is treated like LLM and tool/runtime access
replay/simulation cannot silently perform live host/runtime reads
missing recorded host/runtime outputs fail hard
host/runtime recorder is owned by GameLoop
recorder is propagated to subsystems
snapshots save and restore host runtime recorder state
validator can prove host replay safety
tests exist at unit, functional, and regression level
1) Add the real source file for the host boundary
Create:

src/app/rpg/core/host_runtime_boundary.py

Use the same design shape as tool_runtime_boundary.py.

Required contents

Implement these:

HostCallSpec
HostRuntimeRecord
HostRuntimeRecorder
DeterministicHostRuntimeClient
HostRuntimeGateway
Supported host/runtime nondeterminism classes

Map these operations to effect types:

get_env -> env_read
list_dir -> filesystem_read
read_text -> filesystem_read
wall_time -> wall_clock
monotonic_time -> wall_clock
run_process -> process_spawn
Recorder requirements

HostRuntimeRecorder must support:

make_key(op_name, payload, context=None, config=None)
record(...)
replay(...)
load_records(...)
serialize_state()
deserialize_state(...)

Use stable_json(...) exactly like the other deterministic recorders.

Client requirements

DeterministicHostRuntimeClient.call(...) must:

replay when det.use_recorded_host is true
fail hard with KeyError if replay entry is missing
gate live access through effect_manager.check(...)
execute the live op only when replay is not active
record outputs when det.record_host is true
Gateway requirements

HostRuntimeGateway must support:

set_mode(mode)
set_effect_manager(effect_manager)
set_host_runtime_recorder(recorder)
set_determinism(determinism)
call(op_name, payload, context=None, config=None)
Important behavior

set_mode("replay") and set_mode("simulation") must set:

determinism.replay_mode = True
determinism.use_recorded_host = True

set_mode("live") must set:

determinism.replay_mode = False
determinism.use_recorded_host = False

Do not force record_host; caller controls that.

2) Fully wire host runtime into GameLoop
Modify:

src/app/rpg/core/game_loop.py

Your current loop wiring only handles LLM and tool/runtime. Add host/runtime ownership and propagation.

In __init__

Add constructor param:

host_runtime_recorder: Optional[HostRuntimeRecorder] = None

Store:

self.host_runtime_recorder = host_runtime_recorder or HostRuntimeRecorder()

Import HostRuntimeRecorder.

Propagate to subsystems during init

For each system in:

world
npc_system
story_director
scene_renderer

if it has set_host_runtime_recorder, call it.

Add method
def set_host_runtime_recorder(self, recorder: Any) -> None:
    self.host_runtime_recorder = recorder
    for system_name in ("world", "npc_system", "story_director", "scene_renderer"):
        system = getattr(self, system_name, None)
        if system is not None and hasattr(system, "set_host_runtime_recorder"):
            system.set_host_runtime_recorder(recorder)
Extend mode propagation fallback

Inside the determinism fallback block in set_mode(...), also set:

system.determinism.use_recorded_host = True in replay/simulation
system.determinism.use_recorded_host = False in live

You already do this for LLM and tool runtime. Host must be included too.

Extend explicit effect policies

When constructing EffectPolicy in set_mode(...), explicitly set the new Phase 5.8 host flags.

Live mode

Set all of these to True:

allow_env_read
allow_filesystem_read
allow_wall_clock
allow_process_spawn
Replay/simulation mode

Set all of these to False.

Do not rely on dataclass defaults.

3) Extend SnapshotManager for host runtime recorder
Modify:

src/app/rpg/core/snapshot_manager.py

You already added host_runtime_state as a field. Finish the integration.

Snapshot dataclass

Ensure the field exists:

host_runtime_state: Optional[Dict[str, Any]] = None
save_snapshot(...)

Add:

if hasattr(loop, "host_runtime_recorder") and loop.host_runtime_recorder is not None:
    if hasattr(loop.host_runtime_recorder, "serialize_state"):
        snapshot.host_runtime_state = loop.host_runtime_recorder.serialize_state()
load_snapshot(...)

Add:

if snapshot.host_runtime_state and hasattr(loop, "host_runtime_recorder") and loop.host_runtime_recorder is not None:
    if hasattr(loop.host_runtime_recorder, "deserialize_state"):
        loop.host_runtime_recorder.deserialize_state(snapshot.host_runtime_state)
Doc comments

Update snapshot docs so they explicitly mention:

host_runtime_state: Serialized host runtime recorder state
4) Add host recorder awareness protocol
Modify:

src/app/rpg/core/state_contracts.py

Ensure this exists:

class HostRuntimeRecorderAware(Protocol):
    """Protocol for subsystems that can receive a host/runtime recorder."""

    def set_host_runtime_recorder(self, recorder: Any) -> None:
        """Inject host/runtime recorder for deterministic replay."""
        ...

This should sit beside:

LLMRecorderAware
ToolRuntimeRecorderAware
5) Extend validator for host/runtime replay safety
Modify:

src/app/rpg/validation/state_boundary_validator.py

Add:

def validate_host_runtime_replay_safety(self, gateway: Any) -> Dict[str, Any]:
    """Validate that host/runtime gateway fails on unrecorded replay calls."""
    try:
        gateway.set_mode("replay")
        gateway.call("unrecorded_op", {"x": 1}, context={"test": True})
        return {"ok": False, "reason": "unrecorded replay host call unexpectedly succeeded"}
    except (KeyError, RuntimeError):
        return {"ok": True}
    except Exception as exc:
        return {
            "ok": False,
            "reason": f"unexpected exception type during host replay validation: {type(exc).__name__}: {exc}",
        }

This should match the style of the LLM and tool-runtime validators.

6) Ensure exports are complete and real
Modify:

src/app/rpg/core/__init__.py

Make sure all of these are both imported and exported in __all__:

HostRuntimeRecorderAware
HostCallSpec
HostRuntimeRecord
HostRuntimeRecorder
DeterministicHostRuntimeClient
HostRuntimeGateway

Do not leave dead exports pointing at a missing file.

7) Add Phase 5.8 unit tests
Create:

src/tests/unit/rpg/test_phase58_host_runtime_boundary.py

Use the same structure as the Phase 5.7 tool-runtime tests.

Include a _DummyHostRuntime

It should support:

def call(self, op_name, payload):
    ...
Add these tests
test_live_host_call_blocked_by_effect_policy
allow_env_read=False or appropriate blocked host effect
calling gateway should raise RuntimeError
inner runtime should not be called
test_live_host_call_allowed_and_recorded
enable the right host effect in EffectPolicy
set record_host=True
ensure result returns
ensure one live call happened
ensure one record exists
test_replay_uses_recorded_output_only
live gateway records one host call
replay gateway with use_recorded_host=True returns recorded output
replay runtime inner client must not be called
test_replay_missing_record_fails_hard
replay mode with empty recorder
must raise KeyError
test_set_mode_switches_between_replay_and_live
replay sets use_recorded_host=True
live sets it back to False
test_snapshot_roundtrip_host_runtime_state
recorder records one host interaction
SnapshotManager.save_snapshot(...)
restore into fresh loop-like object
replay succeeds from restored recorder state

Add if __name__ == "__main__": unittest.main().

8) Add Phase 5.8 functional tests
Create:

src/tests/functional/test_phase58_host_runtime_boundary_functional.py

Mirror the style of the Phase 5.7 functional tests.

Build lightweight test doubles

Have:

_Parser
_World
_NPC
_Renderer
_Director
_DummyHostRuntime

The director should call the host gateway during process(...), for example:

result = self.gateway.call("get_env", {"name": "ZONE"}, context={"phase": "director"})
return {"host_result": result}
Required tests
test_replay_blocks_fresh_host_calls_and_uses_recorded
live mode records one host access
replay mode returns same result
inner host runtime call count stays at 1
test_host_runtime_recorder_serialization_roundtrip
record two host operations
serialize and deserialize recorder
ensure replay works
test_replay_missing_host_record_fails_hard
replay mode with empty recorder
host call raises KeyError

Add if __name__ == "__main__": unittest.main().

9) Add Phase 5.8 regression tests
Create:

src/tests/regression/test_phase58_host_runtime_boundary_regression.py

Required tests
test_recorded_outputs_are_snapshot_serializable
record host call
serialize/deserialize recorder
replay must work
test_gateway_no_client_raises_error
gateway without runtime client must raise RuntimeError
test_effect_policy_live_vs_replay_for_host_ops
confirm:
live host-relevant effects can be allowed
replay host-relevant effects are blocked
test_state_boundary_validator_host_replay_safety
use validate_host_runtime_replay_safety(...)
should return {"ok": True}
test_config_sensitive_keying
same op and payload, different config values
replay returns the correct distinct results
test_gateway_mode_switch_preserves_recorded_state
record in live
replay uses recording
no second live runtime call happens

Add if __name__ == "__main__": unittest.main().

10) Optional but recommended: add a concrete dummy subsystem integration path

If any real subsystem is supposed to use host/runtime access, add:

set_host_runtime_recorder(...)
a local HostRuntimeGateway
set_mode(...)
set_effect_manager(...)

But do not spread raw os, time, or subprocess calls around. Keep all such access behind HostRuntimeGateway.

11) Specific consistency rules

The coding LLM must follow these rules while implementing:

A. Do not duplicate boundary logic

Host boundary should match the style and shape of:

llm_boundary.py
tool_runtime_boundary.py
B. Missing replay data must fail hard

Never silently fall back to live host/runtime access in replay or simulation.

C. Snapshots must restore recorder state

The recorder is part of deterministic engine state.

D. Effect policies must be explicit

Do not rely on default values for Phase 5.8 host access.

E. No dead exports

If __init__.py exports a symbol, the file and class must actually exist.

12) Commands the coding LLM should run after applying fixes

Run all of these:

python -m pytest src/tests/unit/rpg/test_phase58_host_runtime_boundary.py -v
python -m pytest src/tests/functional/test_phase58_host_runtime_boundary_functional.py -v
python -m pytest src/tests/regression/test_phase58_host_runtime_boundary_regression.py -v
python -m pytest src/tests/unit/rpg/test_phase57_tool_runtime_boundary.py -v
python -m pytest src/tests/functional/test_phase57_tool_runtime_boundary_functional.py -v
python -m pytest src/tests/regression/test_phase57_tool_runtime_boundary_regression.py -v
python -m pytest src/tests/unit/rpg/test_phase56_llm_boundary.py -v
python -m pytest src/tests/regression/test_phase56_llm_boundary_regression.py -v
python -m pytest src/tests/unit/rpg/test_phase55_state_boundaries.py -v
python -m pytest src/tests/functional/test_phase55_state_boundaries_functional.py -v

If a broader test pass is cheap in your repo, also run:

python -m pytest src/tests/unit/rpg -v
python -m pytest src/tests/functional -v
python -m pytest src/tests/regression -v
13) Expected outcome after the fix

After this patch:

Live mode
host/runtime access is allowed only if effect policy allows it
outputs may be recorded
Replay mode
no fresh host/runtime access
only recorded host/runtime results may be used
missing result causes hard failure
Simulation mode
same as replay for host/runtime access
Snapshots
preserve host runtime recorder state
can restore replay-safe host/runtime context
Validation
can prove host boundary replay safety
14) One more cleanup instruction for the coding LLM

After implementing this phase, search for direct usage of nondeterministic host APIs in the codebase, such as:

os.environ
os.getenv
os.listdir
pathlib.Path.read_text
open(...).read
time.time
time.monotonic
subprocess.run
subprocess.Popen

Any replay-relevant usage should be routed through HostRuntimeGateway or a subsystem-local wrapper backed by DeterministicHostRuntimeClient.