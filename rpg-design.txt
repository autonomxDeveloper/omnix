Phase 8.5 — Save Migration and Packaging Interoperability
Goal

Make saves durable across future phases.

After 8.5, you should be able to:

load older saves after schema changes
export/import a complete RPG package safely
version simulation/player/social/sandbox state
migrate forward deterministically
validate corrupted/incomplete save payloads
keep replay/snapshot compatibility

This phase is about state contract stability.

Design rules
every persisted package must carry a schema version
migrations are forward-only and deterministic
loading must normalize missing keys
export/import format must be explicit and portable
runtime state and package state should be clearly separated
no silent destructive migration
validation errors must be structured
New modules

Create:

src/app/rpg/persistence/
  __init__.py
  save_schema.py
  migration_manager.py
  migrations/
    __init__.py
    v1_to_v2.py
    v2_to_v3.py
  package_builder.py
  package_loader.py
  package_validator.py

src/app/rpg/api/
  rpg_package_routes.py
Canonical package shape

All exported/imported saves should normalize to this top-level shape:

{
    "package_type": "rpg_save_package",
    "schema_version": 3,
    "engine_version": "phase_8_5",
    "created_at": "...",
    "updated_at": "...",
    "adventure": {
        "setup_payload": {...},
        "metadata": {...},
    },
    "state": {
        "simulation_state": {...},
    },
    "artifacts": {
        "snapshots": [],
        "timeline": {},
    },
}

Important:

setup_payload remains the primary gameplay bundle
simulation_state remains the canonical world state
package metadata wraps them for migration/import/export
Versioning policy

Start with:

CURRENT_RPG_SCHEMA_VERSION = 3

Suggested meaning:

v1: pre-player/social/sandbox stabilization
v2: player/social/debug layers normalized
v3: encounter + sandbox + package metadata normalized
File-by-file apply-this-patch spec
1. Create src/app/rpg/persistence/__init__.py
from .save_schema import CURRENT_RPG_SCHEMA_VERSION
from .migration_manager import migrate_package_to_current
from .package_builder import build_save_package
from .package_loader import load_save_package
from .package_validator import validate_save_package

__all__ = [
    "CURRENT_RPG_SCHEMA_VERSION",
    "migrate_package_to_current",
    "build_save_package",
    "load_save_package",
    "validate_save_package",
]
2. Create src/app/rpg/persistence/save_schema.py
from __future__ import annotations

CURRENT_RPG_SCHEMA_VERSION = 3
PACKAGE_TYPE = "rpg_save_package"
ENGINE_VERSION = "phase_8_5"
3. Create src/app/rpg/persistence/migrations/__init__.py
from .v1_to_v2 import migrate_v1_to_v2
from .v2_to_v3 import migrate_v2_to_v3

__all__ = [
    "migrate_v1_to_v2",
    "migrate_v2_to_v3",
]
4. Create src/app/rpg/persistence/migrations/v1_to_v2.py

Purpose:

normalize missing player/debug/social scaffolding
from __future__ import annotations

from typing import Any, Dict


def _safe_dict(v: Any) -> Dict[str, Any]:
    return dict(v) if isinstance(v, dict) else {}


def migrate_v1_to_v2(package: Dict[str, Any]) -> Dict[str, Any]:
    package = dict(package or {})
    state = _safe_dict(package.get("state"))
    simulation_state = _safe_dict(state.get("simulation_state"))

    simulation_state.setdefault("player_state", {})
    simulation_state.setdefault("social_state", {})
    simulation_state.setdefault("debug_meta", {})
    simulation_state.setdefault("gm_overrides", {})

    state["simulation_state"] = simulation_state
    package["state"] = state
    package["schema_version"] = 2
    return package
5. Create src/app/rpg/persistence/migrations/v2_to_v3.py

Purpose:

normalize encounter/sandbox/package artifact structure
from __future__ import annotations

from typing import Any, Dict


def _safe_dict(v: Any) -> Dict[str, Any]:
    return dict(v) if isinstance(v, dict) else {}


def migrate_v2_to_v3(package: Dict[str, Any]) -> Dict[str, Any]:
    package = dict(package or {})
    state = _safe_dict(package.get("state"))
    simulation_state = _safe_dict(state.get("simulation_state"))
    player_state = _safe_dict(simulation_state.get("player_state"))

    player_state.setdefault("encounter_state", {})
    player_state.setdefault("dialogue_state", {})
    simulation_state.setdefault("sandbox_state", {})
    simulation_state["player_state"] = player_state
    state["simulation_state"] = simulation_state
    package["state"] = state

    artifacts = _safe_dict(package.get("artifacts"))
    artifacts.setdefault("snapshots", [])
    artifacts.setdefault("timeline", {})
    package["artifacts"] = artifacts

    package["schema_version"] = 3
    return package
6. Create src/app/rpg/persistence/migration_manager.py
from __future__ import annotations

from typing import Any, Dict

from .save_schema import CURRENT_RPG_SCHEMA_VERSION
from .migrations import migrate_v1_to_v2, migrate_v2_to_v3


def _safe_int(v: Any, default: int = 1) -> int:
    try:
        return int(v)
    except Exception:
        return default


def migrate_package_to_current(package: Dict[str, Any]) -> Dict[str, Any]:
    package = dict(package or {})
    version = _safe_int(package.get("schema_version"), 1)

    while version < CURRENT_RPG_SCHEMA_VERSION:
        if version == 1:
            package = migrate_v1_to_v2(package)
        elif version == 2:
            package = migrate_v2_to_v3(package)
        else:
            raise ValueError(f"Unsupported schema migration path from version {version}")
        version = _safe_int(package.get("schema_version"), version + 1)

    return package
7. Create src/app/rpg/persistence/package_validator.py
from __future__ import annotations

from typing import Any, Dict, List

from .save_schema import PACKAGE_TYPE


def _safe_dict(v: Any) -> Dict[str, Any]:
    return dict(v) if isinstance(v, dict) else {}


def validate_save_package(package: Dict[str, Any]) -> List[Dict[str, str]]:
    package = dict(package or {})
    errors: List[Dict[str, str]] = []

    if package.get("package_type") != PACKAGE_TYPE:
        errors.append({"field": "package_type", "error": "invalid package type"})

    adventure = _safe_dict(package.get("adventure"))
    state = _safe_dict(package.get("state"))

    if not isinstance(adventure.get("setup_payload"), dict):
        errors.append({"field": "adventure.setup_payload", "error": "missing or invalid setup_payload"})

    if not isinstance(state.get("simulation_state"), dict):
        errors.append({"field": "state.simulation_state", "error": "missing or invalid simulation_state"})

    return errors
8. Create src/app/rpg/persistence/package_builder.py
from __future__ import annotations

from typing import Any, Dict
from datetime import datetime, timezone

from .save_schema import (
    CURRENT_RPG_SCHEMA_VERSION,
    PACKAGE_TYPE,
    ENGINE_VERSION,
)


def _utc_now() -> str:
    return datetime.now(timezone.utc).isoformat()


def _safe_dict(v: Any) -> Dict[str, Any]:
    return dict(v) if isinstance(v, dict) else {}


def build_save_package(setup_payload: Dict[str, Any]) -> Dict[str, Any]:
    setup_payload = dict(setup_payload or {})
    metadata = _safe_dict(setup_payload.get("metadata"))
    simulation_state = _safe_dict(metadata.get("simulation_state"))

    return {
        "package_type": PACKAGE_TYPE,
        "schema_version": CURRENT_RPG_SCHEMA_VERSION,
        "engine_version": ENGINE_VERSION,
        "created_at": _utc_now(),
        "updated_at": _utc_now(),
        "adventure": {
            "setup_payload": setup_payload,
            "metadata": metadata,
        },
        "state": {
            "simulation_state": simulation_state,
        },
        "artifacts": {
            "snapshots": list(simulation_state.get("snapshots") or []),
            "timeline": dict(simulation_state.get("timeline") or {}),
        },
    }
9. Create src/app/rpg/persistence/package_loader.py
from __future__ import annotations

from typing import Any, Dict

from .migration_manager import migrate_package_to_current
from .package_validator import validate_save_package


def _safe_dict(v: Any) -> Dict[str, Any]:
    return dict(v) if isinstance(v, dict) else {}


def load_save_package(package: Dict[str, Any]) -> Dict[str, Any]:
    package = migrate_package_to_current(dict(package or {}))
    errors = validate_save_package(package)
    if errors:
        raise ValueError(f"Invalid RPG save package: {errors}")

    adventure = _safe_dict(package.get("adventure"))
    setup_payload = _safe_dict(adventure.get("setup_payload"))
    state = _safe_dict(package.get("state"))
    simulation_state = _safe_dict(state.get("simulation_state"))

    metadata = _safe_dict(setup_payload.get("metadata"))
    metadata["simulation_state"] = simulation_state
    setup_payload["metadata"] = metadata
    return setup_payload
10. Create src/app/rpg/api/rpg_package_routes.py
from __future__ import annotations

from flask import Blueprint, jsonify, request

from app.rpg.persistence import (
    build_save_package,
    load_save_package,
    validate_save_package,
    migrate_package_to_current,
)


rpg_package_bp = Blueprint("rpg_package_bp", __name__)


@rpg_package_bp.post("/api/rpg/package/export")
def export_package():
    data = request.get_json(silent=True) or {}
    setup_payload = dict(data.get("setup_payload") or {})
    package = build_save_package(setup_payload)
    return jsonify({
        "ok": True,
        "package": package,
    })


@rpg_package_bp.post("/api/rpg/package/validate")
def validate_package():
    data = request.get_json(silent=True) or {}
    package = dict(data.get("package") or {})
    migrated = migrate_package_to_current(package)
    errors = validate_save_package(migrated)
    return jsonify({
        "ok": len(errors) == 0,
        "errors": errors,
        "package": migrated,
    })


@rpg_package_bp.post("/api/rpg/package/import")
def import_package():
    data = request.get_json(silent=True) or {}
    package = dict(data.get("package") or {})
    setup_payload = load_save_package(package)
    return jsonify({
        "ok": True,
        "setup_payload": setup_payload,
    })
11. Register the package blueprint

Patch src/app/__init__.py:

diff --git a/src/app/__init__.py b/src/app/__init__.py
index XXXXXXX..PHASE8501 100644
--- a/src/app/__init__.py
+++ b/src/app/__init__.py
@@ -24,6 +24,7 @@ def create_app() -> Flask:
     from app.rpg.api.rpg_player_routes import rpg_player_bp
     from app.rpg.api.rpg_dialogue_routes import rpg_dialogue_bp
     from app.rpg.api.rpg_encounter_routes import rpg_encounter_bp
+    from app.rpg.api.rpg_package_routes import rpg_package_bp
@@ -61,6 +62,7 @@ def create_app() -> Flask:
     app.register_blueprint(rpg_player_bp)
     app.register_blueprint(rpg_dialogue_bp)
     app.register_blueprint(rpg_encounter_bp)
+    app.register_blueprint(rpg_package_bp)
 
     return app
12. Patch src/app/rpg/creator/world_debug.py

Expose package/schema info for GM inspection.

Add in summary payloads where appropriate:

"package_schema_version": 3,

Or better, import CURRENT_RPG_SCHEMA_VERSION and include it in the debug state endpoint response.

13. Optional patch: add save metadata into simulation_state

When stepping simulation, ensure:

history_state.setdefault("save_meta", {})
history_state["save_meta"]["schema_version"] = 3
history_state["save_meta"]["engine_version"] = "phase_8_5"

This is helpful for debugging and migration tracing, but package metadata remains the source of truth.

Test plan

Create:

src/tests/unit/rpg/test_phase85_migration_manager.py
src/tests/unit/rpg/test_phase85_package_builder.py
src/tests/functional/test_phase85_package_routes.py
src/tests/regression/test_phase85_save_compatibility_regression.py

Cover:

Unit
v1 → v2 migration
v2 → v3 migration
migration manager reaches current schema
validator rejects bad package type
builder includes required top-level keys
loader writes simulation_state back into setup_payload
Functional
/api/rpg/package/export
/api/rpg/package/validate
/api/rpg/package/import
Regression
old package fixtures still import
export → import roundtrip preserves simulation_state
package schema version is stable
snapshots/timeline survive roundtrip
Success criteria

After 8.5:

you can safely export/import an RPG save package
older package versions migrate forward deterministically
schema versioning is explicit
invalid packages fail validation clearly
replay/snapshot-bearing saves survive roundtrips
future phases can evolve state with migration hooks instead of brittle ad hoc fixes