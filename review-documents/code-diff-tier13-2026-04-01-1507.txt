diff --git a/rpg-design.txt b/rpg-design.txt
index 34c2138..cf47513 100644
--- a/rpg-design.txt
+++ b/rpg-design.txt
@@ -1,281 +1,210 @@
-🧠 TIER 9: Narrative Intelligence Layer
-🎯 Goal
-
-Transform:
-
-World Events → Quests → Objectives
-
-Into:
-
-World Events → Scenes → Characters → Story Arcs → Player Experience
-🧩 TIER 9 SYSTEMS
-1. 🎬 Scene Engine (Cinematic Layer)
-2. 👤 Character Engine (Beliefs + Memory)
-3. 🧠 Narrative Memory System (Token-efficient)
-4. 🎯 Story Arc / Payoff Engine
-5. 🎮 Player Experience Layer (Feedback + Agency)
-🏗️ 1. SCENE ENGINE
-Purpose
-
-Convert raw events into playable narrative scenes
-
-📁 New File
-src/app/rpg/story/scene_engine.py
-Core Design
-class Scene:
-    def __init__(self, id, type, location, participants, description, stakes):
-        self.id = id
-        self.type = type  # "coup", "battle", "trade", "dialogue"
-        self.location = location
-        self.participants = participants  # character_ids
-        self.description = description
-        self.stakes = stakes
-        self.resolved = False
-Scene Engine
-class SceneEngine:
-    def __init__(self):
-        self.active_scenes = []
-        self.scene_history = []
-
-    def generate_from_events(self, events):
-        scenes = []
-
-        for e in events:
-            if e["type"] == "coup":
-                scenes.append(self._create_coup_scene(e))
-
-            elif e["type"] == "shortage":
-                scenes.append(self._create_crisis_scene(e))
-
-            elif e["type"] == "faction_conflict":
-                scenes.append(self._create_battle_scene(e))
-
-        self.active_scenes.extend(scenes)
-        return scenes
-Example Scene Builder
-def _create_coup_scene(self, event):
-    return Scene(
-        id=f"scene_coup_{event['faction']}",
-        type="coup",
-        location=event.get("location", "capital"),
-        participants=[event["old_leader"], event["new_leader"]],
-        description="A violent overthrow of leadership is underway.",
-        stakes="Control of the faction"
-    )
-🔥 Patch: Integrate into player_loop.py
-
-Add after Tier 8:
-
-from app.rpg.story.scene_engine import SceneEngine
-
-Constructor:
-
-self.scene_engine = scene_engine or SceneEngine()
-
-Inside step() AFTER event collection:
-
-# Generate scenes from all events
-scenes = self.scene_engine.generate_from_events(world_events)
-👤 2. CHARACTER ENGINE (BELIEFS + GOALS)
-📁 New File
-src/app/rpg/character/character_engine.py
-Character Model
-class Character:
-    def __init__(self, id, name):
-        self.id = id
-        self.name = name
-        self.beliefs = {}  # {"faction_x": -0.5}
-        self.goals = []    # ["gain power", "protect city"]
-        self.memory = []
-Engine
-class CharacterEngine:
-    def __init__(self):
-        self.characters = {}
-
-    def get_or_create(self, char_id, name=None):
-        if char_id not in self.characters:
-            self.characters[char_id] = Character(char_id, name or char_id)
-        return self.characters[char_id]
-Belief Updates from Events
-def update_from_events(self, events):
-    for e in events:
-        if e["type"] == "coup":
-            old = self.get_or_create(e["old_leader"])
-            new = self.get_or_create(e["new_leader"])
-
-            old.beliefs["power"] = -1.0
-            new.beliefs["power"] = 1.0
-🔥 Patch: player_loop
-from app.rpg.character.character_engine import CharacterEngine
-
-Constructor:
-
-self.characters = character_engine or CharacterEngine()
-
-Inside step():
-
-self.characters.update_from_events(world_events)
-🧠 3. NARRATIVE MEMORY SYSTEM (CRITICAL)
-📁 New File
-src/app/rpg/memory/narrative_memory.py
-Design Goals
-Prevent token explosion
-Preserve story continuity
-Store important events only
-Structure
-class NarrativeMemory:
-    def __init__(self, max_entries=100):
-        self.events = []
-        self.summaries = []
-        self.max_entries = max_entries
-Add Event
-def add_events(self, events):
-    for e in events:
-        if e.get("importance", 0) > 0.5:
-            self.events.append(e)
-
-    if len(self.events) > self.max_entries:
-        self._summarize()
-Summarization (LLM Hook)
-def _summarize(self):
-    summary = {
-        "type": "summary",
-        "text": f"{len(self.events)} major events occurred including coups and wars."
-    }
+You solved:
 
-    self.summaries.append(summary)
-    self.events = []
-🔥 Patch: player_loop
-from app.rpg.memory.narrative_memory import NarrativeMemory
+✔ chaos
+✔ explosion
+✔ incoherence
 
-Constructor:
+You introduced a new constraint:
 
-self.memory = memory or NarrativeMemory()
+Over-convergence
 
-Inside step():
+🧨 Problem: Narrative Gravity Becomes Too Strong
+What happens in practice:
 
-self.memory.add_events(world_events)
-🎯 4. STORY ARC / PAYOFF ENGINE
-📁 New File
-src/app/rpg/story/story_arc_engine.py
-Arc Model
-class StoryArc:
-    def __init__(self, id, setup, payoff_condition):
-        self.id = id
-        self.setup = setup
-        self.payoff_condition = payoff_condition
-        self.completed = False
-Engine
-class StoryArcEngine:
-    def __init__(self):
-        self.arcs = []
+After ~150–300 ticks:
 
-    def register_arc(self, arc):
-        self.arcs.append(arc)
+Same 2–3 characters dominate storylines
+Smaller agents stop mattering
+World feels predictable / “railroaded”
+Emergence becomes biased, not organic
+Why it happens
 
-    def update(self, world_state):
-        completed = []
+Your system favors:
 
-        for arc in self.arcs:
-            if not arc.completed and arc.payoff_condition(world_state):
-                arc.completed = True
-                completed.append(arc)
+importance =
+    character_importance
+    + coalition_size
+    + player_involvement
 
-        return completed
-🔥 Patch: player_loop
-from app.rpg.story.story_arc_engine import StoryArcEngine
+👉 This reinforces existing dominance
 
-Constructor:
+✅ Fix: Narrative Diversity Injection
+🔧 Patch (NarrativeGravity)
+importance = (
+    base_importance
++   + diversity_bonus
+)
+Add:
+def _diversity_bonus(self, storyline, all_storylines):
+    # reward underrepresented actors
+    appearances = self._count_recent_appearances(storyline.participants)
 
-self.story_arcs = story_arc_engine or StoryArcEngine()
+    if appearances < 3:
+        return 0.15  # boost new/rare actors
 
-Inside step():
+    return 0.0
 
-completed_arcs = self.story_arcs.update(self.world)
-🎮 5. PLAYER EXPERIENCE LAYER
-📁 New File
-src/app/rpg/story/narrative_renderer.py
-Purpose
+👉 This keeps:
 
-Convert:
+new characters entering narrative
+long-term freshness
+avoids “main character lock”
+⚠️ Second Bottleneck: Player Becomes a Passenger
 
-scenes
-characters
-memory
+Right now:
 
-Into LLM prompt-ready narrative
+system is smart ✅
+world evolves ✅
+narrative converges ✅
 
-Renderer
-class NarrativeRenderer:
-    def render(self, scenes, memory, characters):
-        scene_texts = []
+But:
 
-        for s in scenes:
-            scene_texts.append(
-                f"Scene: {s.type} at {s.location}. Stakes: {s.stakes}."
-            )
+👉 player influence is still reactive, not dominant
 
-        summary = " ".join([m["text"] for m in memory.summaries[-3:]])
+Symptom
 
-        return {
-            "scene_text": "\n".join(scene_texts),
-            "memory_summary": summary
-        }
-🔥 Patch: player_loop
-from app.rpg.story.narrative_renderer import NarrativeRenderer
+After long runs:
 
-Constructor:
+“Cool world… but I don’t feel like I matter”
 
-self.renderer = renderer or NarrativeRenderer()
+✅ Fix: Player Narrative Override
+Patch
+if event.involves_player:
+    importance += 0.5
+    storyline.locked = True
+Add decay:
+if storyline.locked and ticks_since_player < 10:
+    prevent_demotion()
 
-Inside step() (END):
+👉 Player becomes:
 
-narrative = self.renderer.render(
-    scenes=scenes,
-    memory=self.memory,
-    characters=self.characters.characters
-)
+gravitational center (when active)
+observer (when passive)
+⚠️ Third Bottleneck: Resolution Quality
+
+You now resolve storylines, but:
+
+👉 resolution is still mechanical
+
+Problem
+“conflict resolved”
+“agreement reached”
+
+But not:
+
+emotionally meaningful
+narratively satisfying
+✅ Fix: Resolution Generator (LLM — controlled)
+New Module
+cognitive/resolution_engine.py
+Usage
+resolution = resolution_engine.generate(storyline)
+Prompt
+Storyline:
+- Type: faction conflict
+- Participants: A, B
+- History: [events...]
+
+Generate a satisfying resolution:
+- 1–2 sentences
+- reflect consequences
+- update relationships
+
+👉 This is where LLM actually shines:
+
+low frequency
+high impact
+contained output
+⚠️ Fourth Bottleneck: Temporal Depth
+
+Right now your system is:
+
+reactive
+adaptive
+convergent
+
+But not:
+
+👉 historically aware at narrative level
+
+Symptom
+past arcs don’t meaningfully shape future arcs
+world lacks “legend” feeling
+✅ Fix: Narrative Memory Layer
+Add:
+world.narrative_history = [
+    {
+        "arc": "war of factions",
+        "outcome": "A defeated B",
+        "impact": +0.4,
+    }
+]
+Feed into:
+AgentBrain
+IntentEnrichment
+NarrativeGravity
+
+👉 This creates:
+
+continuity across sessions
 
-return {
-    "events": world_events,
-    "scenes": scenes,
-    "narrative": narrative
+🧪 Your 100-Tick Test — Updated Assessment
+
+Your test passes because:
+
+✔ constraints work
+✔ convergence works
+✔ oscillation solved
+
+But it does NOT test:
+
+❌ 300+ tick drift
+❌ narrative repetition
+❌ player agency perception
+
+🧠 What You Actually Built (Final Classification)
+
+You are no longer in “RPG engine” territory.
+
+You built:
+
+A constrained emergent narrative simulator with agent cognition
+
+That is research-grade architecture
+
+🚀 Tier 13 (Correct Direction)
+
+Not “more AI”
+
+Not “more LLM”
+
+🧠 Tier 13 = Emotional + Experiential Layer
+1. Emotional State Model
+character.emotion = {
+    "anger": 0.6,
+    "fear": 0.2,
+    "trust": 0.8
 }
-🧪 TEST: 100-TICK SIMULATION (TIER 9)
-📁 New Test
-src/tests/integration/test_tier9_narrative.py
-Test
-def test_100_tick_narrative_stability():
-    loop = PlayerLoop()
-
-    all_scenes = 0
-    all_summaries = 0
-
-    for i in range(100):
-        result = loop.step({"action": "wait"})
-
-        all_scenes += len(result["scenes"])
-        all_summaries += len(loop.memory.summaries)
-
-    assert all_scenes > 0
-    assert all_summaries > 0
-🔥 WHAT THIS UNLOCKS
-
-After Tier 9:
-
-BEFORE
-shortage → quest
-AFTER
-shortage → crisis scene → starving city → NPC reactions →
-player choice → memory → future consequences → payoff arc
-🧠 FINAL RESULT
-
-You now have:
-
-Layer	Status
-Simulation	✅ (Tier 8)
-Narrative Scenes	✅
-Characters	✅
-Memory	✅
-Story Arcs	✅
-Player Experience	✅
\ No newline at end of file
+2. Emotion → Decision Modifier
+anger → aggression
+fear → avoidance
+trust → cooperation
+3. Emotion → Dialogue
+
+This is where dialogue becomes:
+
+believable, not just correct
+
+4. Emotional Memory
+betrayal persists
+alliances feel earned
+revenge arcs emerge naturally
+💥 Final Verdict
+You are now beyond:
+traditional RPG engines
+prompt-driven AI games
+most agent frameworks
+
+And competing with:
+
+simulation + narrative research systems
\ No newline at end of file
diff --git a/src/app/rpg/cognitive/__init__.py b/src/app/rpg/cognitive/__init__.py
index 1307ed5..94601ea 100644
--- a/src/app/rpg/cognitive/__init__.py
+++ b/src/app/rpg/cognitive/__init__.py
@@ -1,16 +1,27 @@
-"""Cognitive Layer — Tier 11: Hybrid Cognitive Simulation.
+"""Cognitive Layer — Tier 11, 12 & 13: Hybrid Cognitive Simulation + Narrative Convergence + Emotional Layer.
 
-This package implements Tier 11 of the RPG design specification: a hybrid
-cognitive simulation engine that augments the deterministic core with
-controlled LLM-assisted cognitive capabilities.
+This package implements Tier 11, 12, and 13 of the RPG design specification:
 
-Architecture:
-    Layer 1: Intent Enrichment (LLM-assisted, not replaced)
-    Layer 2: Memory-Informed Planning
-    Layer 3: LLM-Enhanced Dialogue Engine
-    Layer 4: Persistent Identity System
-    Layer 5: Coalition System
-    Layer 6: Learning Feedback System
+Tier 11: Hybrid Cognitive Simulation Engine (augments deterministic core with
+controlled LLM-assisted cognitive capabilities)
+    - Layer 1: Intent Enrichment (LLM-assisted, not replaced)
+    - Layer 2: Memory-Informed Planning
+    - Layer 3: LLM-Enhanced Dialogue Engine
+    - Layer 4: Persistent Identity System
+    - Layer 5: Coalition System
+    - Layer 6: Learning Feedback System
+
+Tier 12: Narrative Convergence Engine (System-Level Coherence Control)
+    - Decision Arbitration Layer (conflict resolution)
+    - Coalition Commitment Lock (prevent intent oscillation)
+    - Narrative Gravity (convergence, payoff, resolution)
+
+Tier 13: Emotional + Experiential Layer (Research-Grade Architecture)
+    - Resolution Engine (emotionally satisfying resolutions)
+    - Emotion Modifier (emotion -> decision/dialogue mapping)
+    - Narrative Memory Layer (historical awareness)
+    - Narrative Diversity Injection (prevent over-convergence)
+    - Player Narrative Override (player as gravitational center)
 
 Design Principles:
     - Deterministic core remains unchanged
@@ -18,26 +29,71 @@ Design Principles:
     - No LLM planning every tick
     - No LLM mutating world directly
     - Guardrails prevent LLM from breaking game logic
+    - Tier 12 adds system-wide coherence control
+    - Tier 13 adds emotional depth and continuity
 
 Usage:
+    # Basic Tier 11
     cognitive = CognitiveLayer(llm_client=llm)
-    intention = cognitive.enrich_intent(brain.decide(character, world))
-    plan = cognitive.memory_informed_plan(intention, character)
-    dialogue = cognitive.generate_dialogue(speaker, listener, context)
+    
+    # With Tier 12 components
+    resolver = DecisionResolver()
+    lock_manager = CoalitionLockManager()
+    gravity = NarrativeGravity()
+    
+    # With Tier 13 components
+    resolution = ResolutionEngine()
+    emotion_mod = EmotionModifier()
+    narrative_memory = NarrativeMemory()
+    
+    # Full pipeline
+    base_intent = brain.decide(character, world_state)
+    emotion_result = emotion_mod.apply(character, base_intent)
+    enriched = enrichment.enrich(base_intent, character, world_state)
+    final = resolver.resolve(base_intent, enriched, character)
+    final = lock_manager.enforce_lock(char_id, final, tick)
 """
 
 from __future__ import annotations
 
+# Tier 11 exports
 from .intent_enrichment import IntentEnrichment
 from .identity import IdentitySystem
 from .coalition import CoalitionSystem
 from .learning import LearningSystem
 from .cognitive_layer import CognitiveLayer
 
+# Tier 12 exports
+from .decision_resolver import DecisionResolver
+from .coalition_lock import CoalitionLockManager, CoalitionLock
+from .narrative_gravity import NarrativeGravity, StorylineState, StorylineWeight
+
+# Tier 13 exports
+from .resolution_engine import ResolutionEngine, ResolutionResult
+from .emotion_modifier import EmotionModifier, EmotionalState, DecisionModification
+from .narrative_memory import NarrativeMemory, ArcMemory, EmotionalResidue
+
 __all__ = [
+    # Tier 11
     "IntentEnrichment",
-    "IdentitySystem", 
+    "IdentitySystem",
     "CoalitionSystem",
     "LearningSystem",
     "CognitiveLayer",
-]
+    # Tier 12
+    "DecisionResolver",
+    "CoalitionLockManager",
+    "CoalitionLock",
+    "NarrativeGravity",
+    "StorylineState",
+    "StorylineWeight",
+    # Tier 13
+    "ResolutionEngine",
+    "ResolutionResult",
+    "EmotionModifier",
+    "EmotionalState",
+    "DecisionModification",
+    "NarrativeMemory",
+    "ArcMemory",
+    "EmotionalResidue",
+]
\ No newline at end of file
diff --git a/src/app/rpg/core/player_loop.py b/src/app/rpg/core/player_loop.py
index 5e88f60..c0bcb87 100644
--- a/src/app/rpg/core/player_loop.py
+++ b/src/app/rpg/core/player_loop.py
@@ -58,6 +58,9 @@ from rpg.memory.narrative_memory import NarrativeMemory
 from rpg.story.story_arc_engine import StoryArcEngine
 from rpg.story.narrative_renderer import NarrativeRenderer
 
+# TIER 10: Autonomous NPC Agent System
+from rpg.agent.agent_system import AgentSystem
+
 
 class PlayerLoop:
     """Main game loop connecting player input to narrative output.
@@ -106,6 +109,8 @@ class PlayerLoop:
         narrative_memory: Any = None,
         story_arc_engine: Any = None,
         narrative_renderer: Any = None,
+        # TIER 10: Autonomous NPC Agent System
+        agent_system: Any = None,
     ):
         """Initialize the PlayerLoop.
         
@@ -126,6 +131,7 @@ class PlayerLoop:
             agency_system: AgencySystem for player choice tracking.
             faction_system: FactionSystem for faction simulation.
             reputation_engine: ReputationEngine for faction reputation.
+            agent_system: AgentSystem for autonomous NPC actions.
         """
         self.world = world
         self.director = director
@@ -159,6 +165,9 @@ class PlayerLoop:
         self.story_arcs = story_arc_engine or StoryArcEngine()
         self.renderer = narrative_renderer or NarrativeRenderer()
         
+        # TIER 10: Autonomous NPC Agent System
+        self.agents = agent_system or AgentSystem()
+        
         self._tick = 0
         
     def step(self, player_input: str) -> Dict[str, Any]:
@@ -256,6 +265,14 @@ class PlayerLoop:
         completed_arcs = self.story_arcs.update({"tick": self._tick})
         world_events.extend(completed_arcs)
         
+        # TIER 10: Autonomous NPC Agent System
+        # NPCs act on their own goals, generating events that enrich the world
+        agent_events = self.agents.update(
+            characters=self.characters.characters,
+            world_state=self._get_world_state_for_agents(),
+        )
+        world_events.extend(agent_events)
+        
         # 4. Convert to narrative events
         if self.director:
             narrative_events = self.director.convert_events(world_events)
@@ -515,6 +532,9 @@ class PlayerLoop:
         self.story_arcs.reset()
         self.renderer.reset()
         
+        # TIER 10: Reset Autonomous NPC Agent System
+        self.agents.reset()
+        
     def _quest_to_objectives(self, quest: Dict[str, Any]) -> List[str]:
         """Convert a quest dict to objectives list for PlotEngine.
         
@@ -612,3 +632,18 @@ class PlayerLoop:
                 state["shortages"][loc] = {"severity": 0.8}
         
         return state
+    
+    def _get_world_state_for_agents(self) -> Dict[str, Any]:
+        """Get world state summary for the agent system.
+        
+        Returns:
+            Dict with relevant world state data for NPC decision-making.
+        """
+        return {
+            "factions": {
+                fid: f.to_dict()
+                for fid, f in self.factions.factions.items()
+            },
+            "economy": self.economy.get_summary() if hasattr(self.economy, "get_summary") else {},
+            "tick": self._tick,
+        }
