diff --git a/rpg-design.txt b/rpg-design.txt
index 7a656af..c272775 100644
--- a/rpg-design.txt
+++ b/rpg-design.txt
@@ -1,614 +1,645 @@
-Below is the Phase 8.4 — Debug / Analytics / GM Inspection full literal apply-this-patch diff spec, designed to sit on top of your current Phase 7/8/8.5 stack. It assumes your new package/export layer is present and the app is already registering RPG debug/player/dialogue/encounter/package blueprints.
-
 Goal:
 
-replay timeline API
-tick diff engine
-NPC reasoning inspector
-GM intervention hooks
+timeline panel
+latest diff panel
+NPC reasoning drawer
+GM action controls
+debug note UI
+faction trend editor
+tick browser
 
-This phase is about making the machine understandable and steerable.
+This phase is about making the inspection layer actually usable.
 
-1. Create src/app/rpg/analytics/__init__.py
-diff --git a/src/app/rpg/analytics/__init__.py b/src/app/rpg/analytics/__init__.py
-new file mode 100644
---- /dev/null
-+++ b/src/app/rpg/analytics/__init__.py
-@@ -0,0 +1,17 @@
-+from .tick_diff import build_tick_diff
-+from .timeline import build_timeline_summary, get_timeline_tick
-+from .npc_reasoning import inspect_npc_reasoning
-+from .gm_hooks import (
-+    gm_force_npc_goal,
-+    gm_force_faction_trend,
-+    gm_append_debug_note,
-+)
-+
-+__all__ = [
-+    "build_tick_diff",
-+    "build_timeline_summary",
-+    "get_timeline_tick",
-+    "inspect_npc_reasoning",
-+    "gm_force_npc_goal",
-+    "gm_force_faction_trend",
-+    "gm_append_debug_note",
-+]
-2. Create src/app/rpg/analytics/tick_diff.py
-diff --git a/src/app/rpg/analytics/tick_diff.py b/src/app/rpg/analytics/tick_diff.py
+1. Create src/static/rpg/rpgInspectorClient.js
+diff --git a/src/static/rpg/rpgInspectorClient.js b/src/static/rpg/rpgInspectorClient.js
 new file mode 100644
 --- /dev/null
-+++ b/src/app/rpg/analytics/tick_diff.py
-@@ -0,0 +1,148 @@
-+from __future__ import annotations
-+
-+from typing import Any, Dict, List
-+
-+
-+def _safe_dict(v: Any) -> Dict[str, Any]:
-+    return dict(v) if isinstance(v, dict) else {}
-+
-+
-+def _safe_list(v: Any) -> List[Any]:
-+    return list(v) if isinstance(v, list) else []
-+
-+
-+def _safe_int(v: Any, default: int = 0) -> int:
-+    try:
-+        return int(v)
-+    except Exception:
-+        return default
-+
-+
-+def _keys_changed(before: Dict[str, Any], after: Dict[str, Any]) -> List[str]:
-+    all_keys = sorted(set(before.keys()) | set(after.keys()))
-+    changed = []
-+    for key in all_keys:
-+        if before.get(key) != after.get(key):
-+            changed.append(str(key))
-+    return changed
-+
-+
-+def build_tick_diff(before_state: Dict[str, Any], after_state: Dict[str, Any]) -> Dict[str, Any]:
-+    before_state = _safe_dict(before_state)
-+    after_state = _safe_dict(after_state)
-+
-+    before_events = _safe_list(before_state.get("events"))
-+    after_events = _safe_list(after_state.get("events"))
-+    before_consequences = _safe_list(before_state.get("consequences"))
-+    after_consequences = _safe_list(after_state.get("consequences"))
-+
-+    before_social = _safe_dict(before_state.get("social_state"))
-+    after_social = _safe_dict(after_state.get("social_state"))
-+    before_sandbox = _safe_dict(before_state.get("sandbox_state"))
-+    after_sandbox = _safe_dict(after_state.get("sandbox_state"))
-+
-+    before_npc_minds = _safe_dict(before_state.get("npc_minds"))
-+    after_npc_minds = _safe_dict(after_state.get("npc_minds"))
-+
-+    changed_npcs = []
-+    for npc_id in sorted(set(before_npc_minds.keys()) | set(after_npc_minds.keys())):
-+        if before_npc_minds.get(npc_id) != after_npc_minds.get(npc_id):
-+            changed_npcs.append(npc_id)
-+
-+    return {
-+        "tick_before": _safe_int(before_state.get("tick"), 0),
-+        "tick_after": _safe_int(after_state.get("tick"), 0),
-+        "new_events": [
-+            dict(item) for item in after_events[len(before_events):]
-+            if isinstance(item, dict)
-+        ][:20],
-+        "new_consequences": [
-+            dict(item) for item in after_consequences[len(before_consequences):]
-+            if isinstance(item, dict)
-+        ][:20],
-+        "social_keys_changed": _keys_changed(before_social, after_social),
-+        "sandbox_keys_changed": _keys_changed(before_sandbox, after_sandbox),
-+        "changed_npc_ids": changed_npcs[:20],
++++ b/src/static/rpg/rpgInspectorClient.js
+@@ -0,0 +1,87 @@
++export class RPGInspectorClient {
++  async _post(path, payload) {
++    const res = await fetch(path, {
++      method: "POST",
++      headers: { "Content-Type": "application/json" },
++      body: JSON.stringify(payload || {}),
++    });
++    if (!res.ok) {
++      throw new Error(`Inspector request failed: ${res.status}`);
 +    }
-3. Create src/app/rpg/analytics/timeline.py
-diff --git a/src/app/rpg/analytics/timeline.py b/src/app/rpg/analytics/timeline.py
++    return res.json();
++  }
++
++  async getTimeline(setupPayload) {
++    return this._post("/api/rpg/inspect/timeline", {
++      setup_payload: setupPayload,
++    });
++  }
++
++  async getTimelineTick(setupPayload, tick) {
++    return this._post("/api/rpg/inspect/timeline_tick", {
++      setup_payload: setupPayload,
++      tick,
++    });
++  }
++
++  async getTickDiff(beforeState, afterState) {
++    return this._post("/api/rpg/inspect/tick_diff", {
++      before_state: beforeState,
++      after_state: afterState,
++    });
++  }
++
++  async getNpcReasoning(setupPayload, npcId) {
++    return this._post("/api/rpg/inspect/npc_reasoning", {
++      setup_payload: setupPayload,
++      npc_id: npcId,
++    });
++  }
++
++  async forceNpcGoal(setupPayload, npcId, goal) {
++    return this._post("/api/rpg/gm/force_npc_goal", {
++      setup_payload: setupPayload,
++      npc_id: npcId,
++      goal,
++    });
++  }
++
++  async forceFactionTrend(setupPayload, factionId, trendPatch) {
++    return this._post("/api/rpg/gm/force_faction_trend", {
++      setup_payload: setupPayload,
++      faction_id: factionId,
++      trend_patch: trendPatch,
++    });
++  }
++
++  async addDebugNote(setupPayload, note) {
++    return this._post("/api/rpg/gm/debug_note", {
++      setup_payload: setupPayload,
++      note,
++    });
++  }
++}
+2. Create src/static/rpg/rpgInspectorState.js
+diff --git a/src/static/rpg/rpgInspectorState.js b/src/static/rpg/rpgInspectorState.js
 new file mode 100644
 --- /dev/null
-+++ b/src/app/rpg/analytics/timeline.py
-@@ -0,0 +1,93 @@
-+from __future__ import annotations
-+
-+from typing import Any, Dict, List
-+
-+
-+def _safe_dict(v: Any) -> Dict[str, Any]:
-+    return dict(v) if isinstance(v, dict) else {}
-+
-+
-+def _safe_list(v: Any) -> List[Any]:
-+    return list(v) if isinstance(v, list) else []
-+
-+
-+def _safe_int(v: Any, default: int = 0) -> int:
-+    try:
-+        return int(v)
-+    except Exception:
-+        return default
-+
-+
-+def build_timeline_summary(simulation_state: Dict[str, Any]) -> Dict[str, Any]:
-+    simulation_state = _safe_dict(simulation_state)
-+    snapshots = _safe_list(simulation_state.get("snapshots"))
-+    timeline = _safe_dict(simulation_state.get("timeline"))
-+    world_consequences = _safe_list(_safe_dict(simulation_state.get("sandbox_state")).get("world_consequences"))
-+
-+    snapshot_rows = []
-+    for snap in snapshots[:200]:
-+        if not isinstance(snap, dict):
-+            continue
-+        snapshot_rows.append({
-+            "tick": _safe_int(snap.get("tick"), 0),
-+            "snapshot_id": snap.get("snapshot_id", ""),
-+            "label": snap.get("label", ""),
-+        })
-+
-+    snapshot_rows.sort(key=lambda x: (x["tick"], str(x["snapshot_id"])))
-+
-+    return {
-+        "current_tick": _safe_int(simulation_state.get("tick"), 0),
-+        "snapshot_count": len(snapshot_rows),
-+        "snapshots": snapshot_rows[-50:],
-+        "timeline": timeline,
-+        "recent_world_consequences": [
-+            dict(item) for item in world_consequences[-10:]
-+            if isinstance(item, dict)
-+        ],
-+    }
-+
-+
-+def get_timeline_tick(simulation_state: Dict[str, Any], tick: int) -> Dict[str, Any]:
-+    simulation_state = _safe_dict(simulation_state)
-+    snapshots = _safe_list(simulation_state.get("snapshots"))
-+    tick = _safe_int(tick, 0)
-+
-+    exact = None
-+    for snap in snapshots:
-+        if isinstance(snap, dict) and _safe_int(snap.get("tick"), -1) == tick:
-+            exact = dict(snap)
-+            break
-+
-+    return {
-+        "requested_tick": tick,
-+        "snapshot": exact or {},
-+    }
-4. Create src/app/rpg/analytics/npc_reasoning.py
-diff --git a/src/app/rpg/analytics/npc_reasoning.py b/src/app/rpg/analytics/npc_reasoning.py
++++ b/src/static/rpg/rpgInspectorState.js
+@@ -0,0 +1,36 @@
++export const rpgInspectorState = {
++  timeline: null,
++  latestDiff: null,
++  selectedTick: null,
++  selectedTickView: null,
++  selectedNpcId: "",
++  npcReasoning: null,
++  isOpen: false,
++};
++
++export function resetInspectorState() {
++  rpgInspectorState.timeline = null;
++  rpgInspectorState.latestDiff = null;
++  rpgInspectorState.selectedTick = null;
++  rpgInspectorState.selectedTickView = null;
++  rpgInspectorState.selectedNpcId = "";
++  rpgInspectorState.npcReasoning = null;
++}
+3. Create src/static/rpg/rpgInspectorRenderer.js
+diff --git a/src/static/rpg/rpgInspectorRenderer.js b/src/static/rpg/rpgInspectorRenderer.js
 new file mode 100644
 --- /dev/null
-+++ b/src/app/rpg/analytics/npc_reasoning.py
-@@ -0,0 +1,92 @@
-+from __future__ import annotations
-+
-+from typing import Any, Dict, List
-+
-+
-+def _safe_dict(v: Any) -> Dict[str, Any]:
-+    return dict(v) if isinstance(v, dict) else {}
-+
-+
-+def _safe_list(v: Any) -> List[Any]:
-+    return list(v) if isinstance(v, list) else []
-+
-+
-+def _safe_str(v: Any) -> str:
-+    return "" if v is None else str(v)
-+
-+
-+def inspect_npc_reasoning(simulation_state: Dict[str, Any], npc_id: str) -> Dict[str, Any]:
-+    simulation_state = _safe_dict(simulation_state)
-+    npc_index = _safe_dict(simulation_state.get("npc_index"))
-+    npc_minds = _safe_dict(simulation_state.get("npc_minds"))
-+    social_state = _safe_dict(simulation_state.get("social_state"))
-+
-+    npc = _safe_dict(npc_index.get(npc_id))
-+    mind = _safe_dict(npc_minds.get(npc_id))
-+    memory = _safe_dict(mind.get("memory"))
-+    beliefs = _safe_dict(mind.get("beliefs"))
-+    goals = _safe_list(mind.get("goals"))
-+    last_decision = _safe_dict(mind.get("last_decision"))
-+
-+    faction_id = _safe_str(npc.get("faction_id"))
-+    faction_position = _safe_dict(_safe_dict(social_state.get("group_positions")).get(faction_id))
-+
-+    return {
-+        "npc": {
-+            "npc_id": npc_id,
-+            "name": _safe_str(npc.get("name")) or npc_id,
-+            "role": _safe_str(npc.get("role")),
-+            "faction_id": faction_id,
-+            "location_id": _safe_str(npc.get("location_id")),
-+        },
-+        "reasoning": {
-+            "beliefs": beliefs,
-+            "top_goals": goals[:5],
-+            "recent_memories": _safe_list(memory.get("entries"))[:8],
-+            "last_decision": last_decision,
-+            "faction_position": faction_position,
-+        },
-+        "why": {
-+            "player_beliefs": _safe_dict(beliefs.get("player")),
-+            "decision_reason": _safe_str(last_decision.get("reason")),
-+            "dialogue_hint": _safe_str(last_decision.get("dialogue_hint")),
-+        },
-+    }
-5. Create src/app/rpg/analytics/gm_hooks.py
-diff --git a/src/app/rpg/analytics/gm_hooks.py b/src/app/rpg/analytics/gm_hooks.py
++++ b/src/static/rpg/rpgInspectorRenderer.js
+@@ -0,0 +1,232 @@
++function esc(str) {
++  return String(str ?? "")
++    .replaceAll("&", "&amp;")
++    .replaceAll("<", "&lt;")
++    .replaceAll(">", "&gt;")
++    .replaceAll('"', "&quot;")
++    .replaceAll("'", "&#39;");
++}
++
++function safeArray(v) {
++  return Array.isArray(v) ? v : [];
++}
++
++function safeObj(v) {
++  return v && typeof v === "object" ? v : {};
++}
++
++export function renderInspectorShell(isOpen) {
++  const root = document.getElementById("rpg-inspector-shell");
++  if (!root) return;
++  root.style.display = isOpen ? "grid" : "none";
++}
++
++export function renderTimelinePanel(timeline, latestDiff, onSelectTick) {
++  const root = document.getElementById("rpg-inspector-timeline");
++  if (!root) return;
++
++  const t = safeObj(timeline);
++  const snaps = safeArray(t.snapshots);
++  const recentConsequences = safeArray(t.recent_world_consequences);
++  const diff = safeObj(latestDiff);
++
++  root.innerHTML = `
++    <div class="rpg-inspector-section">
++      <div class="rpg-inspector-title">Timeline</div>
++      <div class="rpg-inspector-meta">Current tick: ${esc(t.current_tick ?? "0")}</div>
++      <div class="rpg-inspector-meta">Snapshots: ${esc(t.snapshot_count ?? "0")}</div>
++    </div>
++    <div class="rpg-inspector-section">
++      <div class="rpg-inspector-title">Latest Diff</div>
++      <div class="rpg-inspector-meta">Tick ${esc(diff.tick_before ?? "—")} → ${esc(diff.tick_after ?? "—")}</div>
++      <div class="rpg-inspector-meta">Event Δ: ${esc(safeObj(diff.summary).event_delta ?? "0")}</div>
++      <div class="rpg-inspector-meta">Consequence Δ: ${esc(safeObj(diff.summary).consequence_delta ?? "0")}</div>
++      <div class="rpg-inspector-meta">NPC changes: ${esc(safeObj(diff.summary).npc_changes ?? "0")}</div>
++    </div>
++    <div class="rpg-inspector-section">
++      <div class="rpg-inspector-title">Recent Ticks</div>
++      <div id="rpg-inspector-timeline-list"></div>
++    </div>
++    <div class="rpg-inspector-section">
++      <div class="rpg-inspector-title">Recent World Consequences</div>
++      <div>
++        ${recentConsequences.map(item => `
++          <div class="rpg-inspector-row">
++            <div><strong>${esc(item.type || "consequence")}</strong></div>
++            <div>${esc(item.summary || "")}</div>
++          </div>
++        `).join("")}
++      </div>
++    </div>
++  `;
++
++  const list = document.getElementById("rpg-inspector-timeline-list");
++  if (!list) return;
++
++  list.innerHTML = "";
++  snaps.slice().reverse().forEach((snap) => {
++    const btn = document.createElement("button");
++    btn.className = "rpg-inspector-tick-btn";
++    btn.textContent = `Tick ${snap.tick}`;
++    btn.onclick = () => onSelectTick && onSelectTick(snap.tick);
++    list.appendChild(btn);
++  });
++}
++
++export function renderTickView(tickView) {
++  const root = document.getElementById("rpg-inspector-tick-view");
++  if (!root) return;
++
++  const tv = safeObj(tickView);
++  const snapshot = safeObj(tv.snapshot);
++
++  root.innerHTML = `
++    <div class="rpg-inspector-title">Tick View</div>
++    <div class="rpg-inspector-meta">Requested tick: ${esc(tv.requested_tick ?? "0")}</div>
++    <div class="rpg-inspector-meta">Found: ${esc(tv.found ? "yes" : "no")}</div>
++    <pre class="rpg-inspector-pre">${esc(JSON.stringify(snapshot, null, 2))}</pre>
++  `;
++}
++
++export function renderNpcReasoning(npcReasoning) {
++  const root = document.getElementById("rpg-inspector-npc-reasoning");
++  if (!root) return;
++  const data = safeObj(npcReasoning);
++  const npc = safeObj(data.npc);
++  const reasoning = safeObj(data.reasoning);
++  const why = safeObj(data.why);
++
++  root.innerHTML = `
++    <div class="rpg-inspector-title">NPC Reasoning</div>
++    <div class="rpg-inspector-meta"><strong>${esc(npc.name || npc.npc_id || "NPC")}</strong></div>
++    <div class="rpg-inspector-meta">Role: ${esc(npc.role || "")}</div>
++    <div class="rpg-inspector-meta">Faction: ${esc(npc.faction_id || "")}</div>
++    <div class="rpg-inspector-meta">Location: ${esc(npc.location_id || "")}</div>
++    <div class="rpg-inspector-subtitle">Why</div>
++    <pre class="rpg-inspector-pre">${esc(JSON.stringify(why, null, 2))}</pre>
++    <div class="rpg-inspector-subtitle">Top Goals</div>
++    <pre class="rpg-inspector-pre">${esc(JSON.stringify(reasoning.top_goals || [], null, 2))}</pre>
++    <div class="rpg-inspector-subtitle">Recent Memories</div>
++    <pre class="rpg-inspector-pre">${esc(JSON.stringify(reasoning.recent_memories || [], null, 2))}</pre>
++    <div class="rpg-inspector-subtitle">Last Decision</div>
++    <pre class="rpg-inspector-pre">${esc(JSON.stringify(reasoning.last_decision || {}, null, 2))}</pre>
++  `;
++}
++
++export function renderGmAudit(debugMeta) {
++  const root = document.getElementById("rpg-inspector-gm-audit");
++  if (!root) return;
++  const audit = safeArray(safeObj(debugMeta).gm_audit);
++  const notes = safeArray(safeObj(debugMeta).gm_notes);
++
++  root.innerHTML = `
++    <div class="rpg-inspector-title">GM Audit</div>
++    <div class="rpg-inspector-subtitle">Actions</div>
++    ${audit.slice().reverse().map(item => `
++      <div class="rpg-inspector-row">
++        <div><strong>${esc(item.action || "action")}</strong></div>
++        <div>${esc(JSON.stringify(item))}</div>
++      </div>
++    `).join("")}
++    <div class="rpg-inspector-subtitle">Notes</div>
++    ${notes.slice().reverse().map(item => `
++      <div class="rpg-inspector-row">${esc(item.note || "")}</div>
++    `).join("")}
++  `;
++}
+4. Create src/static/rpg/rpgInspectorUI.js
+diff --git a/src/static/rpg/rpgInspectorUI.js b/src/static/rpg/rpgInspectorUI.js
 new file mode 100644
 --- /dev/null
-+++ b/src/app/rpg/analytics/gm_hooks.py
-@@ -0,0 +1,76 @@
-+from __future__ import annotations
-+
-+from typing import Any, Dict, List
-+
-+
-+def _safe_dict(v: Any) -> Dict[str, Any]:
-+    return dict(v) if isinstance(v, dict) else {}
-+
-+
-+def _safe_list(v: Any) -> List[Any]:
-+    return list(v) if isinstance(v, list) else []
-+
-+
-+def gm_force_npc_goal(simulation_state: Dict[str, Any], npc_id: str, goal: Dict[str, Any]) -> Dict[str, Any]:
-+    simulation_state = dict(simulation_state or {})
-+    npc_minds = simulation_state.setdefault("npc_minds", {})
-+    mind = npc_minds.setdefault(npc_id, {})
-+    goals = _safe_list(mind.get("goals"))
-+    goals.insert(0, dict(goal or {}))
-+    mind["goals"] = goals[:8]
-+    return simulation_state
-+
-+
-+def gm_force_faction_trend(simulation_state: Dict[str, Any], faction_id: str, trend_patch: Dict[str, Any]) -> Dict[str, Any]:
-+    simulation_state = dict(simulation_state or {})
-+    sandbox_state = simulation_state.setdefault("sandbox_state", {})
-+    faction_trends = sandbox_state.setdefault("faction_trends", {})
-+    current = _safe_dict(faction_trends.get(faction_id))
-+    current.update(dict(trend_patch or {}))
-+    faction_trends[faction_id] = current
-+    return simulation_state
-+
-+
-+def gm_append_debug_note(simulation_state: Dict[str, Any], note: str) -> Dict[str, Any]:
-+    simulation_state = dict(simulation_state or {})
-+    debug_meta = simulation_state.setdefault("debug_meta", {})
-+    notes = _safe_list(debug_meta.get("gm_notes"))
-+    notes.append({"note": str(note or "")})
-+    debug_meta["gm_notes"] = notes[-50:]
-+    return simulation_state
-6. Create src/app/rpg/api/rpg_inspection_routes.py
-diff --git a/src/app/rpg/api/rpg_inspection_routes.py b/src/app/rpg/api/rpg_inspection_routes.py
-new file mode 100644
---- /dev/null
-+++ b/src/app/rpg/api/rpg_inspection_routes.py
-@@ -0,0 +1,189 @@
-+from __future__ import annotations
-+
-+from flask import Blueprint, jsonify, request
-+
-+from app.rpg.analytics import (
-+    build_tick_diff,
-+    build_timeline_summary,
-+    get_timeline_tick,
-+    inspect_npc_reasoning,
-+    gm_force_npc_goal,
-+    gm_force_faction_trend,
-+    gm_append_debug_note,
-+)
-+from app.rpg.persistence.save_schema import CURRENT_RPG_SCHEMA_VERSION
-+
-+
-+rpg_inspection_bp = Blueprint("rpg_inspection_bp", __name__)
-+
-+
-+def _get_setup_payload():
-+    data = request.get_json(silent=True) or {}
-+    return dict(data.get("setup_payload") or {})
-+
-+
-+def _get_simulation_state(setup_payload):
-+    meta = dict((setup_payload or {}).get("metadata") or {})
-+    return dict(meta.get("simulation_state") or {})
-+
-+
-+def _write_simulation_state(setup_payload, simulation_state):
-+    setup_payload = dict(setup_payload or {})
-+    meta = dict(setup_payload.get("metadata") or {})
-+    meta["simulation_state"] = dict(simulation_state or {})
-+    setup_payload["metadata"] = meta
-+    return setup_payload
-+
-+
-+@rpg_inspection_bp.post("/api/rpg/inspect/timeline")
-+def inspect_timeline():
-+    setup_payload = _get_setup_payload()
-+    simulation_state = _get_simulation_state(setup_payload)
-+    return jsonify({
-+        "ok": True,
-+        "schema_version": CURRENT_RPG_SCHEMA_VERSION,
-+        "timeline": build_timeline_summary(simulation_state),
-+    })
-+
-+
-+@rpg_inspection_bp.post("/api/rpg/inspect/timeline_tick")
-+def inspect_timeline_tick():
-+    data = request.get_json(silent=True) or {}
-+    setup_payload = dict(data.get("setup_payload") or {})
-+    tick = int(data.get("tick", 0) or 0)
-+    simulation_state = _get_simulation_state(setup_payload)
-+    return jsonify({
-+        "ok": True,
-+        "tick_view": get_timeline_tick(simulation_state, tick),
-+    })
-+
-+
-+@rpg_inspection_bp.post("/api/rpg/inspect/tick_diff")
-+def inspect_tick_diff():
-+    data = request.get_json(silent=True) or {}
-+    before_state = dict(data.get("before_state") or {})
-+    after_state = dict(data.get("after_state") or {})
-+    return jsonify({
-+        "ok": True,
-+        "tick_diff": build_tick_diff(before_state, after_state),
-+    })
-+
-+
-+@rpg_inspection_bp.post("/api/rpg/inspect/npc_reasoning")
-+def inspect_npc():
-+    data = request.get_json(silent=True) or {}
-+    setup_payload = dict(data.get("setup_payload") or {})
-+    npc_id = str(data.get("npc_id") or "")
-+    simulation_state = _get_simulation_state(setup_payload)
-+    return jsonify({
-+        "ok": True,
-+        "npc_reasoning": inspect_npc_reasoning(simulation_state, npc_id),
-+    })
-+
-+
-+@rpg_inspection_bp.post("/api/rpg/gm/force_npc_goal")
-+def force_npc_goal():
-+    data = request.get_json(silent=True) or {}
-+    setup_payload = dict(data.get("setup_payload") or {})
-+    npc_id = str(data.get("npc_id") or "")
-+    goal = dict(data.get("goal") or {})
-+    simulation_state = _get_simulation_state(setup_payload)
-+    simulation_state = gm_force_npc_goal(simulation_state, npc_id, goal)
-+    setup_payload = _write_simulation_state(setup_payload, simulation_state)
-+    return jsonify({
-+        "ok": True,
-+        "setup_payload": setup_payload,
-+    })
-+
-+
-+@rpg_inspection_bp.post("/api/rpg/gm/force_faction_trend")
-+def force_faction_trend():
-+    data = request.get_json(silent=True) or {}
-+    setup_payload = dict(data.get("setup_payload") or {})
-+    faction_id = str(data.get("faction_id") or "")
-+    trend_patch = dict(data.get("trend_patch") or {})
-+    simulation_state = _get_simulation_state(setup_payload)
-+    simulation_state = gm_force_faction_trend(simulation_state, faction_id, trend_patch)
-+    setup_payload = _write_simulation_state(setup_payload, simulation_state)
-+    return jsonify({
-+        "ok": True,
-+        "setup_payload": setup_payload,
-+    })
-+
-+
-+@rpg_inspection_bp.post("/api/rpg/gm/debug_note")
-+def gm_debug_note():
-+    data = request.get_json(silent=True) or {}
-+    setup_payload = dict(data.get("setup_payload") or {})
-+    note = str(data.get("note") or "")
-+    simulation_state = _get_simulation_state(setup_payload)
-+    simulation_state = gm_append_debug_note(simulation_state, note)
-+    setup_payload = _write_simulation_state(setup_payload, simulation_state)
-+    return jsonify({
-+        "ok": True,
-+        "setup_payload": setup_payload,
-+    })
-7. Patch src/app/__init__.py
-diff --git a/src/app/__init__.py b/src/app/__init__.py
-index 5712204..PHASE8401 100644
---- a/src/app/__init__.py
-+++ b/src/app/__init__.py
-@@ -26,6 +26,7 @@ def create_app() -> Flask:
-     from app.rpg.api.rpg_dialogue_routes import rpg_dialogue_bp
-     from app.rpg.api.rpg_encounter_routes import rpg_encounter_bp
-     from app.rpg.api.rpg_package_routes import rpg_package_bp
-+    from app.rpg.api.rpg_inspection_routes import rpg_inspection_bp
-     from app.services import services_bp
-     from app.voice_studio import voice_studio_bp
- 
-@@ -63,6 +64,7 @@ def create_app() -> Flask:
-     app.register_blueprint(rpg_dialogue_bp)
-     app.register_blueprint(rpg_encounter_bp)
-     app.register_blueprint(rpg_package_bp)
-+    app.register_blueprint(rpg_inspection_bp)
- 
-     return app
-8. Patch src/app/rpg/creator/world_simulation.py
++++ b/src/static/rpg/rpgInspectorUI.js
+@@ -0,0 +1,162 @@
++import { RPGInspectorClient } from "./rpgInspectorClient.js";
++import { rpgInspectorState } from "./rpgInspectorState.js";
++import {
++  renderInspectorShell,
++  renderTimelinePanel,
++  renderTickView,
++  renderNpcReasoning,
++  renderGmAudit,
++} from "./rpgInspectorRenderer.js";
++
++const inspectorClient = new RPGInspectorClient();
++
++function getEl(id) {
++  return document.getElementById(id);
++}
++
++export class RPGInspectorUI {
++  constructor(getSetupPayload, getSimulationState) {
++    this.getSetupPayload = getSetupPayload;
++    this.getSimulationState = getSimulationState;
++  }
++
++  toggleOpen() {
++    rpgInspectorState.isOpen = !rpgInspectorState.isOpen;
++    renderInspectorShell(rpgInspectorState.isOpen);
++  }
++
++  async refreshTimeline() {
++    const setupPayload = this.getSetupPayload();
++    const res = await inspectorClient.getTimeline(setupPayload);
++    rpgInspectorState.timeline = res.timeline;
++    rpgInspectorState.latestDiff = res.latest_diff || {};
++    renderTimelinePanel(
++      res.timeline,
++      res.latest_diff || {},
++      async (tick) => {
++        await this.selectTick(tick);
++      }
++    );
++  }
++
++  async selectTick(tick) {
++    const setupPayload = this.getSetupPayload();
++    const res = await inspectorClient.getTimelineTick(setupPayload, tick);
++    rpgInspectorState.selectedTick = tick;
++    rpgInspectorState.selectedTickView = res.tick_view;
++    renderTickView(res.tick_view);
++  }
++
++  async inspectNpc(npcId) {
++    const setupPayload = this.getSetupPayload();
++    const res = await inspectorClient.getNpcReasoning(setupPayload, npcId);
++    rpgInspectorState.selectedNpcId = npcId;
++    rpgInspectorState.npcReasoning = res.npc_reasoning;
++    renderNpcReasoning(res.npc_reasoning);
++  }
++
++  async forceNpcGoal() {
++    const npcId = (getEl("rpg-inspector-npc-id")?.value || "").trim();
++    const goalId = (getEl("rpg-inspector-goal-id")?.value || "").trim();
++    const goalType = (getEl("rpg-inspector-goal-type")?.value || "").trim();
++    const priority = parseFloat((getEl("rpg-inspector-goal-priority")?.value || "1").trim());
++    if (!npcId) return;
++    const setupPayload = this.getSetupPayload();
++    await inspectorClient.forceNpcGoal(setupPayload, npcId, {
++      goal_id: goalId || "gm_forced",
++      type: goalType || "gm_override",
++      priority: isNaN(priority) ? 1.0 : priority,
++    });
++    await this.refreshAudit();
++    await this.inspectNpc(npcId);
++  }
++
++  async forceFactionTrend() {
++    const factionId = (getEl("rpg-inspector-faction-id")?.value || "").trim();
++    const aggression = parseFloat((getEl("rpg-inspector-faction-aggression")?.value || "").trim());
++    const momentum = parseFloat((getEl("rpg-inspector-faction-momentum")?.value || "").trim());
++    if (!factionId) return;
++    const setupPayload = this.getSetupPayload();
++    const patch = {};
++    if (!isNaN(aggression)) patch.aggression = aggression;
++    if (!isNaN(momentum)) patch.momentum = momentum;
++    await inspectorClient.forceFactionTrend(setupPayload, factionId, patch);
++    await this.refreshAudit();
++  }
++
++  async addDebugNote() {
++    const note = (getEl("rpg-inspector-debug-note")?.value || "").trim();
++    if (!note) return;
++    const setupPayload = this.getSetupPayload();
++    await inspectorClient.addDebugNote(setupPayload, note);
++    const input = getEl("rpg-inspector-debug-note");
++    if (input) input.value = "";
++    await this.refreshAudit();
++  }
++
++  async refreshAudit() {
++    const simulationState = this.getSimulationState();
++    renderGmAudit((simulationState || {}).debug_meta || {});
++  }
++
++  bind() {
++    getEl("rpg-inspector-toggle-btn")?.addEventListener("click", () => this.toggleOpen());
++    getEl("rpg-inspector-refresh-btn")?.addEventListener("click", async () => {
++      await this.refreshTimeline();
++      await this.refreshAudit();
++    });
++    getEl("rpg-inspector-inspect-npc-btn")?.addEventListener("click", async () => {
++      const npcId = (getEl("rpg-inspector-npc-id")?.value || "").trim();
++      if (npcId) await this.inspectNpc(npcId);
++    });
++    getEl("rpg-inspector-force-goal-btn")?.addEventListener("click", async () => {
++      await this.forceNpcGoal();
++    });
++    getEl("rpg-inspector-force-faction-btn")?.addEventListener("click", async () => {
++      await this.forceFactionTrend();
++    });
++    getEl("rpg-inspector-add-note-btn")?.addEventListener("click", async () => {
++      await this.addDebugNote();
++    });
++  }
++}
+5. Patch src/static/rpg/rpgPlayerIntegration.js
 
-Add save/debug metadata and persist timeline rows.
+Add inspector bootstrap hooks.
 
-diff --git a/src/app/rpg/creator/world_simulation.py b/src/app/rpg/creator/world_simulation.py
-index 20df2b8..PHASE8402 100644
---- a/src/app/rpg/creator/world_simulation.py
-+++ b/src/app/rpg/creator/world_simulation.py
+diff --git a/src/static/rpg/rpgPlayerIntegration.js b/src/static/rpg/rpgPlayerIntegration.js
+index XXXXXXX..PHASE84601 100644
+--- a/src/static/rpg/rpgPlayerIntegration.js
++++ b/src/static/rpg/rpgPlayerIntegration.js
 @@ -1,5 +1,6 @@
- from __future__ import annotations
+ import { RPGPlayerClient } from "./rpgPlayerClient.js";
+ ...
++import { RPGInspectorUI } from "./rpgInspectorUI.js";
+ ...
+@@ -14,6 +15,7 @@ export class RPGPlayerIntegration {
+     this.setupPayload = setupPayload;
+     this.playerClient = new RPGPlayerClient();
+     this.dialogueClient = new RPGDialogueClient();
+     this.encounterClient = new RPGEncounterClient();
++    this.inspectorUI = null;
+     bindDialogueInput((text) => this.sendDialogueMessage(text));
+   }
+@@ -18,6 +20,21 @@ export class RPGPlayerIntegration {
+   constructor(setupPayload = null) {
  ...
-+from app.rpg.persistence.save_schema import CURRENT_RPG_SCHEMA_VERSION, ENGINE_VERSION
+   }
++
++  ensureInspector() {
++    if (this.inspectorUI) return this.inspectorUI;
++    this.inspectorUI = new RPGInspectorUI(
++      () => this.setupPayload || {},
++      () => {
++        const meta = (this.setupPayload || {}).metadata || {};
++        return meta.simulation_state || {};
++      }
++    );
++    this.inspectorUI.bind();
++    return this.inspectorUI;
++  }
+@@ -58,6 +75,10 @@ export class RPGPlayerIntegration {
+     if (result) {
+       this.setupPayload = result.setup_payload;
  ...
-@@ -780,6 +781,27 @@ def step_simulation_state(setup_payload: dict[str, Any]) -> dict[str, Any]:
-     history_state["sandbox_summary"] = {
-         "projected_outcome_count": len(projected_outcomes),
-         "world_consequence_count": len((history_state.get("sandbox_state") or {}).get("world_consequences") or []),
++      const inspector = this.ensureInspector();
++      await inspector.refreshTimeline();
++      await inspector.refreshAudit();
      }
-+
-+    history_state.setdefault("save_meta", {})
-+    history_state["save_meta"]["schema_version"] = CURRENT_RPG_SCHEMA_VERSION
-+    history_state["save_meta"]["engine_version"] = ENGINE_VERSION
-+
-+    history_state.setdefault("timeline", {})
-+    timeline_events = list((history_state["timeline"].get("ticks") or []))
-+    timeline_events.append({
-+        "tick": int(history_state.get("tick", 0) or 0),
-+        "event_count": len(history_state.get("events") or []),
-+        "consequence_count": len(history_state.get("consequences") or []),
-+        "sandbox_summary": dict(history_state.get("sandbox_summary") or {}),
-+    })
-+    history_state["timeline"]["ticks"] = timeline_events[-200:]
- 
-     # --- Phase 7: GM overrides ---
-     gm_overrides = history_state.get("gm_overrides") or {}
-9. Patch src/app/rpg/creator/world_debug.py
+     return result;
+   }
+
+Repeat that same inspector refresh block after:
 
-Expose schema version and richer summaries.
+startDialogue()
+sendDialogueMessage()
+endDialogueSession()
+startEncounter()
+applyEncounterAction()
+runEncounterNpcTurn()
+endEncounter()
 
-diff --git a/src/app/rpg/creator/world_debug.py b/src/app/rpg/creator/world_debug.py
-index XXXXXXX..PHASE8403 100644
---- a/src/app/rpg/creator/world_debug.py
-+++ b/src/app/rpg/creator/world_debug.py
+Use the same snippet each time:
+
+const inspector = this.ensureInspector();
+await inspector.refreshTimeline();
+await inspector.refreshAudit();
+6. Patch src/static/rpg/rpgMode.js
+
+Boot inspector once on page load.
+
+diff --git a/src/static/rpg/rpgMode.js b/src/static/rpg/rpgMode.js
+index XXXXXXX..PHASE84602 100644
+--- a/src/static/rpg/rpgMode.js
++++ b/src/static/rpg/rpgMode.js
+@@ -1,5 +1,5 @@
+ ...
+ import { RPGPlayerIntegration } from "./rpgPlayerIntegration.js";
+ ...
+@@ -24,6 +24,7 @@ function ensureRpgPlayerIntegration(setupPayload) {
+   if (!rpgPlayerIntegration) {
+     rpgPlayerIntegration = new RPGPlayerIntegration(setupPayload || null);
++    rpgPlayerIntegration.ensureInspector();
+   }
+   if (setupPayload) {
+     rpgPlayerIntegration.setSetupPayload(setupPayload);
+7. Patch src/templates/rpg.html
+
+Add inspector shell and controls.
+
+diff --git a/src/templates/rpg.html b/src/templates/rpg.html
+index XXXXXXX..PHASE84603 100644
+--- a/src/templates/rpg.html
++++ b/src/templates/rpg.html
+@@ -1,6 +1,92 @@
+ ...
++<div class="rpg-inspector-toolbar">
++  <button id="rpg-inspector-toggle-btn" type="button">Inspector</button>
++  <button id="rpg-inspector-refresh-btn" type="button">Refresh Inspector</button>
++</div>
++
++<div id="rpg-inspector-shell" class="rpg-inspector-shell" style="display:none;">
++  <div class="rpg-inspector-column">
++    <div id="rpg-inspector-timeline" class="rpg-inspector-panel"></div>
++    <div id="rpg-inspector-tick-view" class="rpg-inspector-panel"></div>
++  </div>
++  <div class="rpg-inspector-column">
++    <div class="rpg-inspector-panel">
++      <div class="rpg-inspector-title">NPC Inspector</div>
++      <input id="rpg-inspector-npc-id" type="text" placeholder="npc_id" />
++      <button id="rpg-inspector-inspect-npc-btn" type="button">Inspect NPC</button>
++      <div id="rpg-inspector-npc-reasoning"></div>
++    </div>
++    <div class="rpg-inspector-panel">
++      <div class="rpg-inspector-title">Force NPC Goal</div>
++      <input id="rpg-inspector-goal-id" type="text" placeholder="goal_id" />
++      <input id="rpg-inspector-goal-type" type="text" placeholder="goal type" />
++      <input id="rpg-inspector-goal-priority" type="number" step="0.1" placeholder="priority" />
++      <button id="rpg-inspector-force-goal-btn" type="button">Force Goal</button>
++    </div>
++  </div>
++  <div class="rpg-inspector-column">
++    <div class="rpg-inspector-panel">
++      <div class="rpg-inspector-title">Force Faction Trend</div>
++      <input id="rpg-inspector-faction-id" type="text" placeholder="faction_id" />
++      <input id="rpg-inspector-faction-aggression" type="number" step="0.1" placeholder="aggression" />
++      <input id="rpg-inspector-faction-momentum" type="number" step="0.1" placeholder="momentum" />
++      <button id="rpg-inspector-force-faction-btn" type="button">Apply Faction Trend</button>
++    </div>
++    <div class="rpg-inspector-panel">
++      <div class="rpg-inspector-title">Debug Notes</div>
++      <textarea id="rpg-inspector-debug-note" rows="4" placeholder="Enter GM/debug note"></textarea>
++      <button id="rpg-inspector-add-note-btn" type="button">Add Note</button>
++    </div>
++    <div id="rpg-inspector-gm-audit" class="rpg-inspector-panel"></div>
++  </div>
++</div>
++
++<style>
++  .rpg-inspector-toolbar {
++    display: flex;
++    gap: 8px;
++    margin-top: 16px;
++  }
++  .rpg-inspector-shell {
++    display: grid;
++    grid-template-columns: 1fr 1fr 1fr;
++    gap: 12px;
++    margin-top: 12px;
++  }
++  .rpg-inspector-column {
++    min-width: 0;
++  }
++  .rpg-inspector-panel {
++    border: 1px solid #ddd;
++    border-radius: 10px;
++    padding: 12px;
++    background: #fff;
++    margin-bottom: 12px;
++  }
++  .rpg-inspector-title {
++    font-weight: 700;
++    margin-bottom: 8px;
++  }
++  .rpg-inspector-subtitle {
++    font-weight: 600;
++    margin-top: 10px;
++    margin-bottom: 6px;
++  }
++  .rpg-inspector-meta {
++    font-size: 12px;
++    opacity: 0.8;
++    margin-bottom: 4px;
++  }
++  .rpg-inspector-row {
++    padding: 6px 0;
++    border-bottom: 1px solid #eee;
++  }
++  .rpg-inspector-pre {
++    white-space: pre-wrap;
++    word-break: break-word;
++    background: #fafafa;
++    padding: 8px;
++    border-radius: 6px;
++    font-size: 12px;
++  }
++  .rpg-inspector-tick-btn {
++    display: block;
++    width: 100%;
++    text-align: left;
++    margin-bottom: 6px;
++  }
++</style>
+ ...
+8. Optional patch: render schema/save meta in player-facing debug area
+
+If you already expose debug meta somewhere else, add:
+
+diff --git a/src/static/rpg/rpgPlayerRenderer.js b/src/static/rpg/rpgPlayerRenderer.js
+index XXXXXXX..PHASE84604 100644
+--- a/src/static/rpg/rpgPlayerRenderer.js
++++ b/src/static/rpg/rpgPlayerRenderer.js
+@@ -1,5 +1,10 @@
+ ...
++export function renderSaveMeta(simulationState) {
++  const root = document.getElementById("rpg-save-meta");
++  if (!root) return;
++  const meta = ((simulationState || {}).save_meta || {});
++  root.textContent = `schema ${meta.schema_version || "?"} · engine ${meta.engine_version || "?"}`;
++}
+
+And in template:
+
+diff --git a/src/templates/rpg.html b/src/templates/rpg.html
+index PHASE84603..PHASE84605 100644
+--- a/src/templates/rpg.html
++++ b/src/templates/rpg.html
 @@ -1,6 +1,7 @@
- from __future__ import annotations
- from typing import Any, Dict, List
-+from app.rpg.persistence.save_schema import CURRENT_RPG_SCHEMA_VERSION
  ...
-@@ -120,6 +121,7 @@ def summarize_social_state(simulation_state: Dict[str, Any]) -> Dict[str, Any]:
-     return {
-+        "schema_version": CURRENT_RPG_SCHEMA_VERSION,
-         "active_alliances": active_alliances,
-         "active_rumors": active_rumors,
-         "group_positions": {
-             key: dict(value)
-             for key, value in _sorted_dict_items(group_positions)
-10. Optional patch: add world-change summary into narrator metadata
-diff --git a/src/app/rpg/ai/world_scene_narrator.py b/src/app/rpg/ai/world_scene_narrator.py
-index 57c4eb6..PHASE8404 100644
---- a/src/app/rpg/ai/world_scene_narrator.py
-+++ b/src/app/rpg/ai/world_scene_narrator.py
-@@ -657,6 +657,7 @@ class SceneNarrator:
-         "npc_count": len(npc_reactions),
-         "choice_count": len(choices),
-         "player_view": player_view,
-         "sandbox_summary": scene.get("sandbox_summary", {}),
-+        "world_consequences": scene.get("world_consequences", []),
-             },
-         )
-11. Create tests
-src/tests/unit/rpg/test_phase84_tick_diff.py
-diff --git a/src/tests/unit/rpg/test_phase84_tick_diff.py b/src/tests/unit/rpg/test_phase84_tick_diff.py
++<div id="rpg-save-meta" class="rpg-inspector-meta"></div>
+9. Tests
+Create src/tests/unit/rpg/test_phase846_frontend_inspector_files.py
+diff --git a/src/tests/unit/rpg/test_phase846_frontend_inspector_files.py b/src/tests/unit/rpg/test_phase846_frontend_inspector_files.py
 new file mode 100644
 --- /dev/null
-+++ b/src/tests/unit/rpg/test_phase84_tick_diff.py
-@@ -0,0 +1,42 @@
-+from app.rpg.analytics.tick_diff import build_tick_diff
-+
-+
-+def test_build_tick_diff_basic():
-+    before = {"tick": 1, "events": [], "consequences": [], "social_state": {}, "sandbox_state": {}, "npc_minds": {}}
-+    after = {
-+        "tick": 2,
-+        "events": [{"type": "x"}],
-+        "consequences": [{"type": "y"}],
-+        "social_state": {"rumors": []},
-+        "sandbox_state": {"world_consequences": []},
-+        "npc_minds": {"npc1": {"beliefs": {}}},
-+    }
-+    diff = build_tick_diff(before, after)
-+    assert diff["tick_before"] == 1
-+    assert diff["tick_after"] == 2
-+    assert len(diff["new_events"]) == 1
-+    assert len(diff["new_consequences"]) == 1
-+    assert "npc1" in diff["changed_npc_ids"]
-src/tests/unit/rpg/test_phase84_npc_reasoning.py
-diff --git a/src/tests/unit/rpg/test_phase84_npc_reasoning.py b/src/tests/unit/rpg/test_phase84_npc_reasoning.py
-new file mode 100644
---- /dev/null
-+++ b/src/tests/unit/rpg/test_phase84_npc_reasoning.py
-@@ -0,0 +1,39 @@
-+from app.rpg.analytics.npc_reasoning import inspect_npc_reasoning
-+
-+
-+def test_inspect_npc_reasoning_basic():
-+    state = {
-+        "npc_index": {
-+            "npc1": {"name": "Guard", "role": "guard", "faction_id": "watch", "location_id": "gate"}
-+        },
-+        "npc_minds": {
-+            "npc1": {
-+                "beliefs": {"player": {"trust": 0.1}},
-+                "goals": [{"goal_id": "g1"}],
-+                "memory": {"entries": [{"summary": "x"}]},
-+                "last_decision": {"reason": "Suspicious"},
-+            }
-+        },
-+        "social_state": {"group_positions": {"watch": {"stance": "watch"}}},
-+    }
-+    out = inspect_npc_reasoning(state, "npc1")
-+    assert out["npc"]["name"] == "Guard"
-+    assert out["reasoning"]["faction_position"]["stance"] == "watch"
-src/tests/functional/test_phase84_inspection_routes.py
-diff --git a/src/tests/functional/test_phase84_inspection_routes.py b/src/tests/functional/test_phase84_inspection_routes.py
-new file mode 100644
---- /dev/null
-+++ b/src/tests/functional/test_phase84_inspection_routes.py
-@@ -0,0 +1,85 @@
-+import json
-+import pytest
-+
-+from app import create_app
-+
-+
-+@pytest.fixture
-+def client():
-+    app = create_app()
-+    app.config["TESTING"] = True
-+    with app.test_client() as client:
-+        yield client
-+
-+
-+def test_inspect_timeline_route(client):
-+    payload = {"setup_payload": {"metadata": {"simulation_state": {"tick": 1}}}}
-+    res = client.post("/api/rpg/inspect/timeline", data=json.dumps(payload), content_type="application/json")
-+    data = res.get_json()
-+    assert res.status_code == 200
-+    assert data["ok"] is True
-+    assert "timeline" in data
-+
-+
-+def test_inspect_npc_reasoning_route(client):
-+    payload = {
-+        "setup_payload": {
-+            "metadata": {
-+                "simulation_state": {
-+                    "npc_index": {"npc1": {"name": "Guard"}},
-+                    "npc_minds": {"npc1": {"beliefs": {}}},
-+                }
-+            }
-+        },
-+        "npc_id": "npc1",
-+    }
-+    res = client.post("/api/rpg/inspect/npc_reasoning", data=json.dumps(payload), content_type="application/json")
-+    data = res.get_json()
-+    assert res.status_code == 200
-+    assert data["ok"] is True
-+    assert data["npc_reasoning"]["npc"]["name"] == "Guard"
++++ b/src/tests/unit/rpg/test_phase846_frontend_inspector_files.py
+@@ -0,0 +1,34 @@
++import os
++
++
++def test_inspector_client_exists():
++    path = os.path.join(os.path.dirname(__file__), "../../../static/rpg/rpgInspectorClient.js")
++    assert os.path.exists(path)
++    with open(path, "r", encoding="utf-8") as f:
++        content = f.read()
++    assert "class RPGInspectorClient" in content
++
++
++def test_inspector_renderer_exists():
++    path = os.path.join(os.path.dirname(__file__), "../../../static/rpg/rpgInspectorRenderer.js")
++    assert os.path.exists(path)
++    with open(path, "r", encoding="utf-8") as f:
++        content = f.read()
++    assert "renderTimelinePanel" in content
++    assert "renderNpcReasoning" in content
++
++
++def test_inspector_ui_exists():
++    path = os.path.join(os.path.dirname(__file__), "../../../static/rpg/rpgInspectorUI.js")
++    assert os.path.exists(path)
++    with open(path, "r", encoding="utf-8") as f:
++        content = f.read()
++    assert "class RPGInspectorUI" in content
+Optional functional smoke test
+
+Create src/tests/functional/test_phase846_inspector_shell_smoke.py only if you already test rendered templates. Otherwise skip for now.
+
 Success criteria
 
 After this patch:
 
-you can inspect timeline summaries via API
-you can request a specific tick snapshot
-you can diff before/after state payloads
-you can inspect NPC reasoning directly
-GMs can force NPC goals and faction trends
-GMs can attach debug notes
-schema/version info is visible in inspection surfaces
\ No newline at end of file
+inspector opens/closes in the UI
+timeline summary is visible
+latest diff is visible
+recent ticks are clickable
+tick detail view renders
+NPC reasoning can be queried from the UI
+GM can force NPC goals from the UI
+GM can force faction trend changes from the UI
+GM can add debug notes from the UI
+GM audit trail is visible
+
+This is the right frontend capstone for the 8.4 backend stack you already completed.
\ No newline at end of file
diff --git a/src/static/rpg/rpgPlayerIntegration.js b/src/static/rpg/rpgPlayerIntegration.js
index 87eb63a..d82689d 100644
--- a/src/static/rpg/rpgPlayerIntegration.js
+++ b/src/static/rpg/rpgPlayerIntegration.js
@@ -12,14 +12,39 @@ import { renderPlayerView } from "./rpgPlayerRenderer.js";
 import { loadJournal, loadCodex, loadObjectives, handleEnterDialogue, handleExitDialogue, refreshSidePanels, bindDialogueInput } from "./rpgPlayerUI.js";
 import { renderDialogueState, renderSuggestedReplies, appendDialogueReply, clearDialogueUI } from "./rpgDialogueRenderer.js";
 
+// Phase 8.4.6 — Inspector integration
+import { RPGInspectorUI } from "./rpgInspectorUI.js";
+
 export class RPGPlayerIntegration {
   constructor(setupPayload = null) {
     this.setupPayload = setupPayload;
     this.playerClient = new RPGPlayerClient();
     this.dialogueClient = new RPGDialogueClient();
+    this.inspectorUI = null; // Phase 8.4.6 — Inspector UI instance
     bindDialogueInput((text) => this.sendDialogueMessage(text));
   }
 
+  // Phase 8.4.6 — Bootstrap hooks for inspector
+  ensureInspector() {
+    if (this.inspectorUI) return this.inspectorUI;
+    this.inspectorUI = new RPGInspectorUI(
+      () => this.setupPayload || {},
+      () => {
+        const meta = (this.setupPayload || {}).metadata || {};
+        return meta.simulation_state || {};
+      }
+    );
+    this.inspectorUI.bind();
+    return this.inspectorUI;
+  }
+
+  // Phase 8.4.6 — Helper to refresh inspector after state changes
+  async _refreshInspector() {
+    const inspector = this.ensureInspector();
+    await inspector.refreshTimeline();
+    await inspector.refreshAudit();
+  }
+
   setSetupPayload(payload) {
     this.setupPayload = payload;
   }
@@ -55,6 +80,7 @@ export class RPGPlayerIntegration {
     if (result) {
       this.setupPayload = result.setupPayload;
       rpgPlayerState.playerState = result.playerState;
+      await this._refreshInspector();
     }
     return result;
   }
@@ -71,6 +97,7 @@ export class RPGPlayerIntegration {
     if (result) {
       this.setupPayload = result.setupPayload;
       rpgPlayerState.playerState = result.playerState;
+      await this._refreshInspector();
     }
     return result;
   }
@@ -84,6 +111,7 @@ export class RPGPlayerIntegration {
       rpgPlayerState.playerState.dialogue_state = result.dialogue_state;
       renderDialogueState(result.dialogue_state);
       renderSuggestedReplies(result.dialogue_state, (text) => this.sendDialogueMessage(text));
+      await this._refreshInspector();
     }
     return result;
   }
@@ -110,6 +138,7 @@ export class RPGPlayerIntegration {
       renderDialogueState(result.dialogue_state);
       renderSuggestedReplies(result.dialogue_state, (text) => this.sendDialogueMessage(text));
       appendDialogueReply(result.reply);
+      await this._refreshInspector();
     }
     return result;
   }
@@ -122,6 +151,7 @@ export class RPGPlayerIntegration {
       rpgPlayerState.playerState = rpgPlayerState.playerState || {};
       rpgPlayerState.playerState.dialogue_state = result.dialogue_state;
       clearDialogueUI();
+      await this._refreshInspector();
     }
     return result;
   }
